INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 05 / 05 / 2026
BIMESTRE: 2º Bimestre

DESCRIÇÃO DA ATIVIDADE:
Formulários.

// Importa o React e o hook useState (para criar estados)
import React, { useState } from 'react';

// Importa componentes básicos do React Native
import { View, Text, TextInput, Button, StyleSheet, Alert } from 'react-native';

// Componente principal do app
export default function App() {

  // Criação dos estados para armazenar os valores digitados
  const [nome, setNome] = useState(''); // Estado para o nome
  const [email, setEmail] = useState(''); // Estado para o e-mail
  const [cpf, setCpf] = useState(''); // Estado para o CPF
  const [data, setData] = useState(''); // Estado para a data de nascimento

  // Função chamada quando o botão é pressionado
  const enviar = () => {

    // Verifica se algum campo está vazio
    if (!nome || !email || !cpf || !data) {

      // Mostra um alerta de erro
      Alert.alert("Erro", "Preencha todos os campos!");
      return; // Interrompe a função
    }

    // Se todos os campos estiverem preenchidos
    Alert.alert("Sucesso", "Inscrição realizada!");
  };

  // Interface do app
  return (
    <View style={styles.container}> {/* Container principal */}

      {/* Título da tela */}
      <Text style={styles.titulo}>Inscreva-se</Text>

      {/* Campo de entrada para o nome */}
      <TextInput
        style={styles.input}
        placeholder="Nome"
        value={nome} // valor ligado ao estado
        onChangeText={setNome} // atualiza o estado ao digitar
      />

      {/* Campo de entrada para o e-mail */}
      <TextInput
        style={styles.input}
        placeholder="E-mail"
        value={email}
        onChangeText={setEmail}
      />

      {/* Campo de entrada para o CPF */}
      <TextInput
        style={styles.input}
        placeholder="CPF"
        value={cpf}
        onChangeText={setCpf}
      />

      {/* Campo de entrada para data de nascimento */}
      <TextInput
        style={styles.input}
        placeholder="Data de Nascimento"
        value={data}
        onChangeText={setData}
      />

      {/* Botão que chama a função enviar */}
      <Button title="Inscrever-se" onPress={enviar} />
    </View>
  );
}

// Criação dos estilos
const styles = StyleSheet.create({

  // Estilo do container principal
  container: {
    flex: 1, // ocupa toda a tela
    justifyContent: 'center', // centraliza verticalmente
    padding: 20, // espaço interno
  },

  // Estilo do título
  titulo: {
    fontSize: 24, // tamanho da fonte
    marginBottom: 20, // espaço abaixo
    textAlign: 'center', // centraliza o texto
  },

  // Estilo dos inputs
  input: {
    borderWidth: 1, // borda
    borderColor: '#ccc', // cor da borda
    padding: 10, // espaço interno
    marginBottom: 10, // espaço entre inputs
    borderRadius: 5, // bordas arredondadas
  },
});

***************************************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A):Rakely da Silva de Souza
DADOS: 12 / 05 / 2026
BIMESTRE: 2º Bimestre

DESCRIÇÃO DE ATIVIDADE:
Arvore de Letras

import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function App() {

return (

<View style={styles.container}>

<View style={styles.linha}>
<Text style={[styles.caixa, {backgroundColor: '#FFD6E0'}]}>A</Text>
</View>

<View style={styles.linha}>
<Text style={[styles.caixa, {backgroundColor: '#FFE5B4'}]}>B</Text>
<Text style={[styles.caixa, {backgroundColor: '#FFF3B0'}]}>C</Text>
</View>

<View style={styles.linha}>
<Text style={[styles.caixa, {backgroundColor: '#D9F7BE'}]}>D</Text>
<Text style={[styles.caixa, {backgroundColor: '#B5EAD7'}]}>E</Text>
<Text style={[styles.caixa, {backgroundColor: '#C7CEEA'}]}>F</Text>
</View>

<View style={styles.linha}>
<Text style={[styles.caixa, {backgroundColor: '#E2C2FF'}]}>G</Text>
<Text style={[styles.caixa, {backgroundColor: '#FFDAC1'}]}>H</Text>
<Text style={[styles.caixa, {backgroundColor: '#BDE0FE'}]}>I</Text>
<Text style={[styles.caixa, {backgroundColor: '#F1C0E8'}]}>J</Text>
</View>

<View style={styles.linha}>
<Text style={[styles.caixa, {backgroundColor: '#CDEAC0'}]}>K</Text>
<Text style={[styles.caixa, {backgroundColor: '#A0E7E5'}]}>L</Text>
</View>

</View>

);

}

const styles = StyleSheet.create({

container: {
flex: 1,
justifyContent: 'center',
alignItems: 'center'
},

linha: {
flexDirection: 'row'
},

caixa: {
color: '#555',
padding: 20,
margin: 5,
fontSize: 20,
fontWeight: 'bold',
borderRadius: 10
}

});

************************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 05 / 2026
BIMESTRE: 2º Bimestre

DESCRIÇÃO DA ATIVIDADE:
Projeto de tela no snack

import React from 'react';
import {
View,
Text,
StyleSheet,
TouchableOpacity,
Image,
Dimensions,
ScrollView,
Alert,
} from 'react-native';

const largura = Dimensions.get('window').width;

export default function App() {

function mostrarMensagem(tema) {

let mensagem = '';

switch (tema) {
case 'HTML':
mensagem = 'HTML é usado para criar a estrutura de sites.';
break;

case 'CSS':
mensagem = 'CSS serve para estilizar páginas e deixar tudo bonito.';
break;

case 'React':
mensagem = 'React ajuda a criar interfaces modernas e rápidas.';
break;

case 'Mobile':
mensagem = 'Mobile é o desenvolvimento de aplicativos para celular.';
break;

case 'Lógica':
mensagem = 'Lógica de programação ajuda a resolver problemas.';
break;

case 'Web':
mensagem = 'Web envolve criação de sites e sistemas online.';
break;

case 'API':
mensagem = 'API conecta sistemas diferentes e troca informações.';
break;

case 'UI Design':
mensagem = 'UI Design cuida da aparência e experiência visual.';
break;
}

Alert.alert(tema, mensagem);
}

return (
<View style={styles.container}>
<ScrollView contentContainerStyle={styles.scrollContent}>

{/* Imagem no topo */}
<Image
source={{
uri: 'https://static.vecteezy.com/system/resources/previews/049/873/333/non_2x/blue-lilies-love-photo.jpeg',
}}
style={styles.imagemTopo}
/>

{/* Título */}
<Text style={styles.titulo}>Aprendendo Juntos</Text>

{/* Subtítulo */}
<Text style={styles.subtitulo}>
💙
</Text>

{/* Cards */}
<View style={styles.areaCards}>

<TouchableOpacity
style={styles.card}
onPress={() => mostrarMensagem('HTML')}
>
<Text style={styles.icone}>💻</Text>
<Text style={styles.textoCard}>HTML</Text>
</TouchableOpacity>

<TouchableOpacity
style={styles.card}
onPress={() => mostrarMensagem('CSS')}
>
<Text style={styles.icone}>🎨</Text>
<Text style={styles.textoCard}>CSS</Text>
</TouchableOpacity>

<TouchableOpacity
style={styles.card}
onPress={() => mostrarMensagem('React')}
>
<Text style={styles.icone}>⚛️</Text>
<Text style={styles.textoCard}>React</Text>
</TouchableOpacity>

<TouchableOpacity
style={styles.card}
onPress={() => mostrarMensagem('Mobile')}
>
<Text style={styles.icone}>📱</Text>
<Text style={styles.textoCard}>Mobile</Text>
</TouchableOpacity>

<TouchableOpacity
style={styles.card}
onPress={() => mostrarMensagem('Lógica')}
>
<Text style={styles.icone}>🧠</Text>
<Text style={styles.textoCard}>Lógica</Text>
</TouchableOpacity>

<TouchableOpacity
style={styles.card}
onPress={() => mostrarMensagem('Web')}
>
<Text style={styles.icone}>🌐</Text>
<Text style={styles.textoCard}>Web</Text>
</TouchableOpacity>

<TouchableOpacity
style={styles.card}
onPress={() => mostrarMensagem('API')}
>
<Text style={styles.icone}>☁️</Text>
<Text style={styles.textoCard}>API</Text>
</TouchableOpacity>

<TouchableOpacity
style={styles.card}
onPress={() => mostrarMensagem('UI Design')}
>
<Text style={styles.icone}>✨</Text>
<Text style={styles.textoCard}>UI Design</Text>
</TouchableOpacity>

</View>

{/* Botão */}
<TouchableOpacity style={styles.botao}>
<Text style={styles.textoBotao}>COMEÇAR AGORA</Text>
</TouchableOpacity>

</ScrollView>
</View>
);
}

const styles = StyleSheet.create({

container: {
flex: 1,
backgroundColor: '#EAF6FF',
},

scrollContent: {
alignItems: 'center',
paddingVertical: 1,
paddingHorizontal: 15,
},

imagemTopo: {
width: '120%',
height: 200,
marginBottom: 30,
resizeMode:'cover',
},

titulo: {
fontSize: 28,
fontWeight: 'bold',
color: '#4B7EA8',
marginBottom: 8,
},

subtitulo: {
fontSize: 15,
color: '#6E91B3',
marginBottom: 30,
textAlign: 'center',
},

areaCards: {
flexDirection: 'row',
flexWrap: 'wrap',
justifyContent: 'center',
},

card: {
width: largura * 0.38,
backgroundColor: '#CFE9FF',
padding: 18,
margin: 10,
borderRadius: 20,
alignItems: 'center',
shadowColor: '#000',
shadowOpacity: 0.1,
shadowRadius: 5,
elevation: 4,
},

icone: {
fontSize: 30,
marginBottom: 10,
},

textoCard: {
fontSize: 16,
fontWeight: '600',
color: '#3C6E91',
},

botao: {
marginTop: 30,
backgroundColor: '#7DB7E8',
paddingVertical: 15,
paddingHorizontal: 40,
borderRadius: 30,
marginBottom: 40,
},

textoBotao: {
color: '#fff',
fontWeight: 'bold',
fontSize: 16,
},

});

*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 19 / 05 / 2026
BIMESTRE: 2º Bimestre

DESCRIÇÃO DA ATIVIDADE:
Revisão

import React from 'react';
import { Text, View } from 'react-native';

export default function App() {

  return (

    // Caixa principal
    <View style={{ marginTop: 100, alignItems: 'center' }}>

      {/* Texto com estilos de fonte */}
      <Text
        style={{

          fontSize: 30, // Tamanho da fonte

          color: 'black', // Cor da letra

          fontWeight: 'bold', // Negrito

          fontFamily: 'serif', // Fonte normal do sistema
          
          fontWeight: '200'
          

          /*fontStyle: 'italic' // Itálico*/

        }}
      >
        Baby,
      </Text>


      {/* Texto com estilos de fonte */}
      <Text
        style={{

          fontSize: 30, // Tamanho da fonte

          color: 'blue', // Cor da letra

          fontWeight: 'bold', // Negrito

          fontFamily: 'monospace', // Fonte normal do sistema
          
          fontWeight: '200'
          

          /*fontStyle: 'italic' // Itálico*/

        }}
      >
        Now that i found you
      </Text>

       {/* Texto com estilos de fonte */}
      <Text
        style={{

          fontSize: 30, // Tamanho da fonte

          color: 'red', // Cor da letra

          fontWeight: 'bold', // Negrito

          fontFamily: 'sans-serif', // Fonte normal do sistema
          
          fontWeight: '100'
          

          /*fontStyle: 'italic' // Itálico*/

        }}
      >
        i can't let you gooooooo
      </Text>
    </View>

    

  );
}

***************************************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A):Rakely da Silva de Souza
DADOS: 02 / 06 / 2026
BIMESTRE: 2º Bimestre

DESCRIÇÃO DE ATIVIDADE:
Cadastro de produtos

import React, { useState } from 'react';

import {
View,
Text,
TextInput,
TouchableOpacity,
Alert,
StyleSheet,
ScrollView,
} from 'react-native';

export default function App() {

// =========================
// ESTADOS
// =========================

const [nome, setNome] = useState('');
const [preco, setPreco] = useState('');
const [categoria, setCategoria] = useState('Roupas');
const [descricao, setDescricao] = useState('');

// Lista de produtos
const [produtos, setProdutos] = useState([]);

// =========================
// CADASTRAR PRODUTO
// =========================

const cadastrarProduto = () => {

// Verifica campos vazios
if (
nome.trim() === '' ||
preco.trim() === '' ||
descricao.trim() === ''
) {

Alert.alert(
'Erro',
'Preencha todos os campos.'
);

return;
}

// Verifica se o preço é válido
if (isNaN(preco)) {

Alert.alert(
'Erro',
'Digite um preço válido.'
);

return;
}

// Novo produto
const novoProduto = {
id: Date.now().toString(),
nome,
preco,
categoria,
descricao,
};

// Adiciona na lista
setProdutos((prev) => [...prev, novoProduto]);

// Mensagem
Alert.alert(
'Produto cadastrado',
`${nome} foi cadastrado com sucesso!`
);

// Limpa os campos
limparCamposSemMensagem();
};

// =========================
// DELETAR PRODUTO
// =========================

const deletarProduto = (id, nomeProduto) => {

// Remove da lista
const novaLista = produtos.filter(
(item) => item.id !== id
);

setProdutos(novaLista);

// Mensagem
Alert.alert(
'Produto removido',
`${nomeProduto} foi excluído.`
);
};

// =========================
// LIMPAR CAMPOS
// =========================

const limparCampos = () => {

setNome('');
setPreco('');
setCategoria('Roupas');
setDescricao('');

Alert.alert(
'Campos limpos',
'Todos os campos foram apagados.'
);
};

// Limpa sem mostrar mensagem
const limparCamposSemMensagem = () => {

setNome('');
setPreco('');
setCategoria('Roupas');
setDescricao('');
};

// =========================
// TELA
// =========================

return (

<View style={styles.container}>

{/* CABEÇALHO */}

<View style={styles.header}>

<Text style={styles.titulo}>
Cadastro de Produtos
</Text>

<Text style={styles.subtitulo}>
Cadatre seu produto
</Text>

</View>

{/* SCROLL */}

<ScrollView
showsVerticalScrollIndicator={false}
>

{/* FORMULÁRIO */}

<View style={styles.formulario}>

{/* NOME */}

<Text style={styles.label}>
Nome do Produto
</Text>

<TextInput
style={styles.input}
placeholder="Digite o nome"
placeholderTextColor="#8B7355"
value={nome}
onChangeText={setNome}
/>

{/* PREÇO */}

<Text style={styles.label}>
Preço
</Text>

<TextInput
style={styles.input}
placeholder="Digite o preço"
placeholderTextColor="#8B7355"
keyboardType="numeric"
value={preco}
onChangeText={setPreco}
/>

{/* CATEGORIA */}

<Text style={styles.label}>
Categoria
</Text>

<View style={styles.categoriasContainer}>

{[
'Roupas',
'Alimentos',
'Games',
'Outros',
].map((item) => (

<TouchableOpacity
key={item}

style={[
styles.botaoCategoria,

categoria === item &&
styles.categoriaAtiva,
]}

onPress={() => {

setCategoria(item);

Alert.alert(
'Categoria selecionada',
`Você escolheu ${item}`
);
}}
>

<Text
style={[
styles.textoCategoria,

categoria === item &&
styles.textoCategoriaAtivo,
]}
>
{item}
</Text>

</TouchableOpacity>

))}

</View>

{/* DESCRIÇÃO */}

<Text style={styles.label}>
Descrição
</Text>

<TextInput
style={[
styles.input,
styles.inputDescricao,
]}
placeholder="Digite a descrição"
placeholderTextColor="#8B7355"
multiline
value={descricao}
onChangeText={setDescricao}
/>

{/* BOTÃO CADASTRAR */}

<TouchableOpacity
style={styles.botaoCadastrar}
onPress={cadastrarProduto}
>

<Text style={styles.textoBotao}>
CADASTRAR
</Text>

</TouchableOpacity>

{/* BOTÃO LIMPAR */}

<TouchableOpacity
style={styles.botaoLimpar}
onPress={limparCampos}
>

<Text style={styles.textoBotao}>
LIMPAR
</Text>

</TouchableOpacity>

</View>

{/* =========================
LISTA DE PRODUTOS
========================= */}

<View style={styles.listaContainer}>

<Text style={styles.listaTitulo}>
Produtos Cadastrados
</Text>

{/* Se não houver produtos */}

{produtos.length === 0 ? (

<Text style={styles.semProdutos}>
Nenhum produto cadastrado.
</Text>

) : (

produtos.map((item) => (

<View
key={item.id}
style={styles.cardProduto}
>

{/* Nome */}
<Text style={styles.nomeProduto}>
{item.nome}
</Text>

{/* Preço */}
<Text style={styles.precoProduto}>
R$ {item.preco}
</Text>

{/* Categoria */}
<Text style={styles.categoriaProduto}>
{item.categoria}
</Text>

{/* Descrição */}
<Text style={styles.descricaoProduto}>
{item.descricao}
</Text>

{/* BOTÃO EXCLUIR */}

<TouchableOpacity
style={styles.botaoExcluir}

onPress={() =>
deletarProduto(
item.id,
item.nome
)
}
>

<Text style={styles.textoBotao}>
EXCLUIR
</Text>

</TouchableOpacity>

</View>
))

)}

</View>

</ScrollView>

</View>
);
}

// =========================
// ESTILOS
// =========================

const styles = StyleSheet.create({

// Fundo principal
container: {
flex: 1,
backgroundColor: '#F5E6D3',
},

// Cabeçalho
header: {
backgroundColor: '#B08968',
paddingTop: 55,
paddingBottom: 25,
alignItems: 'center',
},

// Título
titulo: {
fontSize: 28,
fontWeight: 'bold',
color: '#FFF8F0',
},

// Subtítulo
subtitulo: {
fontSize: 15,
color: '#FDF6EC',
marginTop: 5,
},

// Área formulário
formulario: {
padding: 20,
},

// Labels
label: {
fontSize: 16,
fontWeight: 'bold',
color: '#6F4E37',
marginBottom: 8,
},

// Inputs
input: {
backgroundColor: '#FFF8F0',
borderWidth: 1,
borderColor: '#D2B48C',
borderRadius: 12,
padding: 14,
marginBottom: 18,
fontSize: 15,
color: '#5C4033',
},

// Input descrição
inputDescricao: {
height: 100,
textAlignVertical: 'top',
},

// Categorias
categoriasContainer: {
flexDirection: 'row',
flexWrap: 'wrap',
gap: 10,
marginBottom: 20,
},

// Botão categoria
botaoCategoria: {
backgroundColor: '#E6CCB2',
paddingVertical: 10,
paddingHorizontal: 15,
borderRadius: 10,
},

// Categoria ativa
categoriaAtiva: {
backgroundColor: '#7F5539',
},

// Texto categoria
textoCategoria: {
color: '#5C4033',
fontWeight: 'bold',
},

// Texto categoria ativo
textoCategoriaAtivo: {
color: '#FFFFFF',
},

// Botão cadastrar
botaoCadastrar: {
backgroundColor: '#9C6644',
padding: 15,
borderRadius: 14,
alignItems: 'center',
marginBottom: 12,
},

// Botão limpar
botaoLimpar: {
backgroundColor: '#D4A373',
padding: 15,
borderRadius: 14,
alignItems: 'center',
},

// Texto botão
textoBotao: {
color: '#FFFFFF',
fontWeight: 'bold',
fontSize: 15,
},

// Lista
listaContainer: {
paddingHorizontal: 20,
paddingBottom: 30,
},

// Título lista
listaTitulo: {
fontSize: 24,
fontWeight: 'bold',
color: '#6F4E37',
marginBottom: 20,
},

// Sem produtos
semProdutos: {
textAlign: 'center',
color: '#8B7355',
marginTop: 20,
fontSize: 16,
},

// Card produto
cardProduto: {
backgroundColor: '#FFF8F0',
padding: 18,
borderRadius: 16,
marginBottom: 15,
borderLeftWidth: 6,
borderLeftColor: '#9C6644',
},

// Nome produto
nomeProduto: {
fontSize: 20,
fontWeight: 'bold',
color: '#5C4033',
marginBottom: 8,
},

// Preço produto
precoProduto: {
fontSize: 17,
fontWeight: 'bold',
color: '#7F5539',
marginBottom: 5,
},

// Categoria produto
categoriaProduto: {
color: '#8B7355',
marginBottom: 5,
},

// Descrição produto
descricaoProduto: {
color: '#6F4E37',
marginBottom: 15,
},

// Botão excluir
botaoExcluir: {
backgroundColor: '#A47148',
padding: 13,
borderRadius: 10,
alignItems: 'center',
},

});

********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 16 / 06 / 2026
BIMESTRE: 2º Bimestre

DESCRIÇÃO DA ATIVIDADE:
mini projeto, verificação de idade

import React, { useState } from "react";
import { View, TextInput, Button, Alert } from "react-native";

export default function App() {
  const [idade, setIdade] = useState("");

  function validarIdade() {
    try {
      const valor = Number(idade);

      if (valor < 18) {
        throw new Error("Menor de idade. Entrada não permitida.");
      }

      Alert.alert("Sucesso", "Entrada liberada!");
    } catch (erro) {
      Alert.alert("Erro", erro.message);
    } finally {
      console.log("Verificação concluída.");
    }
  }

  return (
    <View style={{ padding: 20, marginTop: 50 }}>
      <TextInput
        placeholder="Digite sua idade"
        keyboardType="numeric"
        value={idade}
        onChangeText={setIdade}
        style={{ borderWidth: 1, marginBottom: 10, padding: 10 }}
      />
      <Button title="Validar Idade" onPress={validarIdade} />
    </View>
  );
}

********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 16 / 06 / 2026
BIMESTRE: 2º Bimestre

DESCRIÇÃO DA ATIVIDADE:
mini projeto, saque bancário

import React, { useState } from 'react';
import { View, Text, TextInput, Button, StyleSheet, Alert } from 'react-native';

export default function App() {
  const [saldo, setSaldo] = useState('1000');
  const [saque, setSaque] = useState('');
  const [resultado, setResultado] = useState('');

  const realizarSaque = () => {
    try {
      const saldoAtual = parseFloat(saldo);
      const valorSaque = parseFloat(saque);

      if (isNaN(valorSaque) || valorSaque <= 0) {
        throw new Error('Digite um valor de saque válido.');
      }

      if (valorSaque > saldoAtual) {
        throw new Error('Saldo insuficiente para realizar o saque.');
      }

      const novoSaldo = saldoAtual - valorSaque;

      setSaldo(novoSaldo.toString());
      setResultado(
        `Saque realizado com sucesso!\nNovo saldo: R$ ${novoSaldo.toFixed(2)}`
      );
    } catch (erro) {
      Alert.alert('Erro', erro.message);
      setResultado(`Erro: ${erro.message}`);
    } finally {
      console.log('Operação de saque finalizada.');
    }
  };

  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Sistema Bancário</Text>

      <Text style={styles.label}>Saldo Atual (R$)</Text>
      <TextInput
        style={styles.input}
        value={saldo}
        editable={false}
      />

      <Text style={styles.label}>Valor do Saque (R$)</Text>
      <TextInput
        style={styles.input}
        keyboardType="numeric"
        value={saque}
        onChangeText={setSaque}
        placeholder="Digite o valor"
      />

      <Button title="Realizar Saque" onPress={realizarSaque} />

      <Text style={styles.resultado}>{resultado}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    justifyContent: 'center',
  },
  titulo: {
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 20,
  },
  label: {
    fontSize: 16,
    marginTop: 10,
  },
  input: {
    borderWidth: 1,
    borderColor: '#999',
    borderRadius: 5,
    padding: 10,
    marginBottom: 10,
  },
  resultado: {
    marginTop: 20,
    fontSize: 16,
    textAlign: 'center',
  },
});
****************************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 16 / 06 / 2026
BIMESTRE: 2º Bimestre

DESCRIÇÃO DA ATIVIDADE:
mini projeto, nota escolar

import React, { useState } from 'react';
import { View, Text, TextInput, Button, StyleSheet, Alert } from 'react-native';

export default function App() {
  const [nota, setNota] = useState('');
  const [resultado, setResultado] = useState('');

  const validarNota = () => {
    try {
      const valorNota = parseFloat(nota);

      if (isNaN(valorNota)) {
        throw new Error('Digite uma nota válida.');
      }

      if (valorNota < 0) {
        throw new Error('Erro: a nota não pode ser menor que 0.');
      }

      if (valorNota > 10) {
        throw new Error('Erro: a nota não pode ser maior que 10.');
      }

      setResultado(`Nota válida: ${valorNota}`);
    } catch (erro) {
      Alert.alert('Erro', erro.message);
      setResultado(erro.message);
    } finally {
      console.log('Validação finalizada.');
    }
  };

  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Validação de Nota Escolar</Text>

      <TextInput
        style={styles.input}
        placeholder="Digite a nota"
        keyboardType="numeric"
        value={nota}
        onChangeText={setNota}
      />

      <Button title="Validar Nota" onPress={validarNota} />

      <Text style={styles.resultado}>{resultado}</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    padding: 20,
  },
  titulo: {
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 20,
  },
  input: {
    borderWidth: 1,
    borderColor: '#999',
    borderRadius: 5,
    padding: 10,
    marginBottom: 15,
  },
  resultado: {
    marginTop: 20,
    fontSize: 18,
    textAlign: 'center',
  },
});

*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 08 / 2026
BIMESTRE: 3º Bimestre
Descrição: Revisão, Cadastro Inteligente de Usuários

import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
} from 'react-native';

export default function App() {
  const [nome, setNome] = useState('');
  const [email, setEmail] = useState('');
  const [idade, setIdade] = useState('');
  const [mensagem, setMensagem] = useState('');
  const [erro, setErro] = useState(false);

  function cadastrar() {
    setMensagem('');
    setErro(false);

    // Validação do nome
    if (nome.trim() === '') {
      setMensagem('Por favor, informe seu nome.');
      setErro(true);
      return;
    }

    // Validação do e-mail
    if (email.trim() === '') {
      setMensagem('Por favor, informe seu e-mail.');
      setErro(true);
      return;
    }

    // Validação do formato do e-mail
    if (!email.includes('@') || !email.includes('.')) {
      setMensagem('Informe um e-mail válido.');
      setErro(true);
      return;
    }

    // Validação da idade
    if (idade.trim() === '') {
      setMensagem('Por favor, informe sua idade.');
      setErro(true);
      return;
    }

    if (isNaN(Number(idade)) || Number(idade) <= 0) {
      setMensagem('Informe uma idade válida.');
      setErro(true);
      return;
    }

    // Cadastro realizado
    setMensagem(
      `Cadastro realizado com sucesso!\n\n` +
      `Olá, ${nome}! 👋\n` +
      `E-mail: ${email}\n` +
      `Idade: ${idade} anos.`
    );
  }

  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Cadastro de Participante</Text>

      <Text style={styles.label}>Nome</Text>
      <TextInput
        style={styles.input}
        placeholder="Digite seu nome"
        value={nome}
        onChangeText={setNome}
      />

      <Text style={styles.label}>E-mail</Text>
      <TextInput
        style={styles.input}
        placeholder="Digite seu e-mail"
        keyboardType="email-address"
        autoCapitalize="none"
        value={email}
        onChangeText={setEmail}
      />

      <Text style={styles.label}>Idade</Text>
      <TextInput
        style={styles.input}
        placeholder="Digite sua idade"
        keyboardType="numeric"
        value={idade}
        onChangeText={setIdade}
      />

      <TouchableOpacity
        style={styles.botao}
        onPress={cadastrar}
      >
        <Text style={styles.textoBotao}>CADASTRAR</Text>
      </TouchableOpacity>

      {mensagem !== '' && (
        <Text style={[styles.mensagem, erro && styles.erro]}>
          {mensagem}
        </Text>
      )}
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f2f4f8',
    padding: 25,
    justifyContent: 'center',
  },

  titulo: {
    fontSize: 25,
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 30,
    color: '#222',
  },

  label: {
    fontSize: 16,
    fontWeight: 'bold',
    marginBottom: 5,
    color: '#333',
  },

  input: {
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 8,
    padding: 12,
    fontSize: 16,
    marginBottom: 15,
  },

  botao: {
    backgroundColor: '#2563eb',
    padding: 15,
    borderRadius: 8,
    alignItems: 'center',
    marginTop: 10,
  },

  textoBotao: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
  },

  mensagem: {
    marginTop: 20,
    padding: 15,
    borderRadius: 8,
    backgroundColor: '#dcfce7',
    color: '#166534',
    fontSize: 16,
    textAlign: 'center',
  },

  erro: {
    backgroundColor: '#fee2e2',
    color: '#b91c1c',
  },
});


*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 08 / 2026
BIMESTRE: 3º Bimestre
Descrição: Revisão, Calculadora de IMC

import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  KeyboardAvoidingView,
  Platform,
  Alert,
} from 'react-native';

export default function App() {
  const [peso, setPeso] = useState('');
  const [altura, setAltura] = useState('');
  const [resultado, setResultado] = useState(null);

  function calcularIMC() {
    const pesoNumerico = parseFloat(peso.replace(',', '.'));
    const alturaNumerica = parseFloat(altura.replace(',', '.'));

    // Validação dos campos
    if (!pesoNumerico || !alturaNumerica || pesoNumerico <= 0 || alturaNumerica <= 0) {
      Alert.alert(
        'Atenção',
        'Digite um peso e uma altura válidos.'
      );
      return;
    }

    // Cálculo do IMC
    const imc = pesoNumerico / (alturaNumerica * alturaNumerica);

    // Classificação do IMC
    let classificacao;

    if (imc < 18.5) {
      classificacao = 'Abaixo do peso';
    } else if (imc < 25) {
      classificacao = 'Peso normal';
    } else if (imc < 30) {
      classificacao = 'Sobrepeso';
    } else if (imc < 35) {
      classificacao = 'Obesidade grau I';
    } else if (imc < 40) {
      classificacao = 'Obesidade grau II';
    } else {
      classificacao = 'Obesidade grau III';
    }

    setResultado({
      imc: imc.toFixed(2),
      classificacao: classificacao,
    });
  }

  return (
    <KeyboardAvoidingView
      style={styles.container}
      behavior={Platform.OS === 'ios' ? 'padding' : undefined}
    >
      <View style={styles.card}>
        <Text style={styles.titulo}>Calculadora de IMC</Text>

        <Text style={styles.subtitulo}>
          Informe seus dados para calcular o Índice de Massa Corporal.
        </Text>

        <Text style={styles.label}>Peso (kg)</Text>

        <TextInput
          style={styles.input}
          placeholder="Ex.: 60"
          keyboardType="decimal-pad"
          value={peso}
          onChangeText={setPeso}
        />

        <Text style={styles.label}>Altura (m)</Text>

        <TextInput
          style={styles.input}
          placeholder="Ex.: 1.65"
          keyboardType="decimal-pad"
          value={altura}
          onChangeText={setAltura}
        />

        <TouchableOpacity
          style={styles.botao}
          onPress={calcularIMC}
        >
          <Text style={styles.textoBotao}>CALCULAR IMC</Text>
        </TouchableOpacity>

        {resultado && (
          <View style={styles.resultado}>
            <Text style={styles.tituloResultado}>Resultado</Text>

            <Text style={styles.imc}>
              IMC: {resultado.imc}
            </Text>

            <Text style={styles.classificacao}>
              {resultado.classificacao}
            </Text>
          </View>
        )}
      </View>
    </KeyboardAvoidingView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EAF4F4',
    justifyContent: 'center',
    padding: 20,
  },

  card: {
    backgroundColor: '#FFFFFF',
    borderRadius: 20,
    padding: 25,
    elevation: 5,
    shadowColor: '#000',
    shadowOpacity: 0.15,
    shadowRadius: 8,
    shadowOffset: {
      width: 0,
      height: 3,
    },
  },

  titulo: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#176B87',
    textAlign: 'center',
    marginBottom: 10,
  },

  subtitulo: {
    fontSize: 15,
    color: '#666',
    textAlign: 'center',
    marginBottom: 25,
  },

  label: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#333',
    marginBottom: 8,
  },

  input: {
    height: 50,
    borderWidth: 1,
    borderColor: '#B8C8CC',
    borderRadius: 10,
    paddingHorizontal: 15,
    fontSize: 16,
    marginBottom: 18,
    backgroundColor: '#FAFAFA',
  },

  botao: {
    backgroundColor: '#176B87',
    height: 52,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 5,
  },

  textoBotao: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: 'bold',
  },

  resultado: {
    marginTop: 25,
    padding: 20,
    backgroundColor: '#F0F8FA',
    borderRadius: 12,
    alignItems: 'center',
  },

  tituloResultado: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#333',
    marginBottom: 8,
  },

  imc: {
    fontSize: 26,
    fontWeight: 'bold',
    color: '#176B87',
    marginBottom: 5,
  },

  classificacao: {
    fontSize: 18,
    fontWeight: 'bold',
    color: '#333',
    textAlign: 'center',
  },
});


*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 08 / 2026
BIMESTRE: 3º Bimestre
Descrição: Revisão, Conversor de temperatura

import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  Alert,
} from 'react-native';

export default function App() {
  const [celsius, setCelsius] = useState('');
  const [fahrenheit, setFahrenheit] = useState('');

  // Função responsável pela conversão
  function converterTemperatura() {
    const valor = parseFloat(celsius.replace(',', '.'));

    // Validação do campo
    if (isNaN(valor)) {
      Alert.alert(
        'Atenção',
        'Digite uma temperatura válida em Celsius.'
      );
      return;
    }

    // Fórmula: F = (C × 9/5) + 32
    const resultado = (valor * 9) / 5 + 32;

    setFahrenheit(resultado.toFixed(2));
  }

  // Função responsável por limpar os campos
  function limparCampos() {
    setCelsius('');
    setFahrenheit('');
  }

  return (
    <View style={styles.container}>
      <View style={styles.card}>
        <Text style={styles.titulo}>
          Conversor de Temperatura
        </Text>

        <Text style={styles.subtitulo}>
          Converta temperaturas de Celsius para Fahrenheit
        </Text>

        <Text style={styles.label}>
          Temperatura em Celsius (°C)
        </Text>

        <TextInput
          style={styles.input}
          placeholder="Ex.: 25"
          keyboardType="decimal-pad"
          value={celsius}
          onChangeText={setCelsius}
        />

        <TouchableOpacity
          style={styles.botao}
          onPress={converterTemperatura}
        >
          <Text style={styles.textoBotao}>
            CONVERTER
          </Text>
        </TouchableOpacity>

        {fahrenheit !== '' && (
          <View style={styles.resultado}>
            <Text style={styles.labelResultado}>
              Resultado:
            </Text>

            <Text style={styles.temperatura}>
              {fahrenheit} °F
            </Text>
          </View>
        )}

        <TouchableOpacity
          style={styles.botaoLimpar}
          onPress={limparCampos}
        >
          <Text style={styles.textoBotaoLimpar}>
            LIMPAR
          </Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EAF4F4',
    justifyContent: 'center',
    padding: 20,
  },

  card: {
    backgroundColor: '#FFFFFF',
    borderRadius: 20,
    padding: 25,
    elevation: 5,
    shadowColor: '#000',
    shadowOpacity: 0.15,
    shadowRadius: 8,
    shadowOffset: {
      width: 0,
      height: 3,
    },
  },

  titulo: {
    fontSize: 27,
    fontWeight: 'bold',
    color: '#176B87',
    textAlign: 'center',
    marginBottom: 10,
  },

  subtitulo: {
    fontSize: 15,
    color: '#666',
    textAlign: 'center',
    marginBottom: 25,
  },

  label: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#333',
    marginBottom: 8,
  },

  input: {
    height: 50,
    borderWidth: 1,
    borderColor: '#B8C8CC',
    borderRadius: 10,
    paddingHorizontal: 15,
    fontSize: 16,
    marginBottom: 18,
    backgroundColor: '#FAFAFA',
  },

  botao: {
    backgroundColor: '#176B87',
    height: 52,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 5,
  },

  textoBotao: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: 'bold',
  },

  resultado: {
    marginTop: 25,
    padding: 20,
    backgroundColor: '#F0F8FA',
    borderRadius: 12,
    alignItems: 'center',
  },

  labelResultado: {
    fontSize: 16,
    color: '#555',
    marginBottom: 5,
  },

  temperatura: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#176B87',
  },

  botaoLimpar: {
    marginTop: 15,
    height: 50,
    borderRadius: 10,
    borderWidth: 2,
    borderColor: '#176B87',
    justifyContent: 'center',
    alignItems: 'center',
  },

  textoBotaoLimpar: {
    color: '#176B87',
    fontSize: 16,
    fontWeight: 'bold',
  },
});


*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 08 / 2026
BIMESTRE: 3º Bimestre
Descrição: Revisão,  Sistema de Login

import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  Alert,
} from 'react-native';

export default function App() {
  const [usuario, setUsuario] = useState('');
  const [senha, setSenha] = useState('');

  // Função responsável por verificar o login
  function entrar() {
    if (usuario === 'admin' && senha === '1234') {
      Alert.alert(
        'Acesso autorizado',
        'Bem-vindo, admin!'
      );
    } else {
      Alert.alert(
        'Acesso negado',
        'Usuário ou senha incorretos.'
      );
    }
  }

  return (
    <View style={styles.container}>
      <View style={styles.card}>

        <Text style={styles.titulo}>
          Acesso ao Sistema
        </Text>

        <Text style={styles.subtitulo}>
          Informe seus dados para entrar
        </Text>

        <Text style={styles.label}>
          Usuário
        </Text>

        <TextInput
          style={styles.input}
          placeholder="Digite seu usuário"
          value={usuario}
          onChangeText={setUsuario}
          autoCapitalize="none"
        />

        <Text style={styles.label}>
          Senha
        </Text>

        <TextInput
          style={styles.input}
          placeholder="Digite sua senha"
          value={senha}
          onChangeText={setSenha}
          secureTextEntry
        />

        <TouchableOpacity
          style={styles.botao}
          onPress={entrar}
        >
          <Text style={styles.textoBotao}>
            ENTRAR
          </Text>
        </TouchableOpacity>

      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EAF4F4',
    justifyContent: 'center',
    padding: 20,
  },

  card: {
    backgroundColor: '#FFFFFF',
    borderRadius: 20,
    padding: 25,
    elevation: 5,
    shadowColor: '#000',
    shadowOpacity: 0.15,
    shadowRadius: 8,
    shadowOffset: {
      width: 0,
      height: 3,
    },
  },

  titulo: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#176B87',
    textAlign: 'center',
    marginBottom: 10,
  },

  subtitulo: {
    fontSize: 15,
    color: '#666',
    textAlign: 'center',
    marginBottom: 25,
  },

  label: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#333',
    marginBottom: 8,
  },

  input: {
    height: 50,
    borderWidth: 1,
    borderColor: '#B8C8CC',
    borderRadius: 10,
    paddingHorizontal: 15,
    fontSize: 16,
    marginBottom: 18,
    backgroundColor: '#FAFAFA',
  },

  botao: {
    backgroundColor: '#176B87',
    height: 52,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 5,
  },

  textoBotao: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: 'bold',
  },
});

*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 08 / 2026
BIMESTRE: 3º Bimestre
Descrição: Revisão, Simulador de Caixa Eletrônico

import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  Alert,
} from 'react-native';

export default function App() {
  const [valor, setValor] = useState('');

  function verificarSaque() {
    const valorNumerico = parseFloat(valor.replace(',', '.'));

    if (isNaN(valorNumerico) || valorNumerico <= 0) {
      Alert.alert('Valor inválido', 'Digite um valor válido.');
      return;
    }

    if (valorNumerico % 10 === 0) {
      Alert.alert('Saque autorizado', 'O valor pode ser sacado.');
    } else {
      Alert.alert(
        'Valor inválido',
        'O valor do saque deve ser múltiplo de R$ 10.'
      );
    }
  }

  return (
    <View style={styles.container}>
      <View style={styles.card}>
        <Text style={styles.titulo}>Caixa Eletrônico</Text>

        <Text style={styles.label}>Valor do saque</Text>

        <TextInput
          style={styles.input}
          placeholder="Ex.: 100"
          keyboardType="numeric"
          value={valor}
          onChangeText={setValor}
        />

        <TouchableOpacity
          style={styles.botao}
          onPress={verificarSaque}
        >
          <Text style={styles.textoBotao}>VERIFICAR SAQUE</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EAF4F4',
    justifyContent: 'center',
    padding: 20,
  },

  card: {
    backgroundColor: '#FFF',
    borderRadius: 20,
    padding: 25,
    elevation: 5,
  },

  titulo: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#176B87',
    textAlign: 'center',
    marginBottom: 25,
  },

  label: {
    fontSize: 16,
    fontWeight: 'bold',
    marginBottom: 8,
  },

  input: {
    height: 50,
    borderWidth: 1,
    borderColor: '#B8C8CC',
    borderRadius: 10,
    paddingHorizontal: 15,
    fontSize: 16,
    marginBottom: 20,
  },

  botao: {
    backgroundColor: '#176B87',
    height: 52,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },

  textoBotao: {
    color: '#FFF',
    fontWeight: 'bold',
    fontSize: 16,
  },
});

*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 08 / 2026
BIMESTRE: 3º Bimestre
Descrição: Revisão, Aplicativo de Lista de Compras

import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  FlatList,
  StyleSheet,
} from 'react-native';

export default function App() {
  const [produto, setProduto] = useState('');
  const [lista, setLista] = useState([]);

  function adicionarProduto() {
    if (produto.trim() === '') {
      return;
    }

    setLista([...lista, produto.trim()]);
    setProduto('');
  }

  function limparLista() {
    setLista([]);
  }

  return (
    <View style={styles.container}>
      <View style={styles.card}>
        <Text style={styles.titulo}>Lista de Compras</Text>

        <TextInput
          style={styles.input}
          placeholder="Digite um produto"
          value={produto}
          onChangeText={setProduto}
        />

        <TouchableOpacity
          style={styles.botao}
          onPress={adicionarProduto}
        >
          <Text style={styles.textoBotao}>ADICIONAR PRODUTO</Text>
        </TouchableOpacity>

        <Text style={styles.subtitulo}>
          Produtos cadastrados:
        </Text>

        <FlatList
          data={lista}
          keyExtractor={(item, index) => index.toString()}
          renderItem={({ item, index }) => (
            <View style={styles.item}>
              <Text style={styles.textoItem}>
                {index + 1}. {item}
              </Text>
            </View>
          )}
          ListEmptyComponent={
            <Text style={styles.vazio}>
              Nenhum produto cadastrado.
            </Text>
          }
        />

        <TouchableOpacity
          style={styles.botaoLimpar}
          onPress={limparLista}
        >
          <Text style={styles.textoLimpar}>LIMPAR LISTA</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EAF4F4',
    padding: 20,
    justifyContent: 'center',
  },

  card: {
    backgroundColor: '#FFF',
    borderRadius: 20,
    padding: 25,
    maxHeight: '90%',
    elevation: 5,
  },

  titulo: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#176B87',
    textAlign: 'center',
    marginBottom: 20,
  },

  input: {
    height: 50,
    borderWidth: 1,
    borderColor: '#B8C8CC',
    borderRadius: 10,
    paddingHorizontal: 15,
    marginBottom: 15,
  },

  botao: {
    backgroundColor: '#176B87',
    height: 50,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },

  textoBotao: {
    color: '#FFF',
    fontWeight: 'bold',
  },

  subtitulo: {
    fontSize: 18,
    fontWeight: 'bold',
    marginTop: 25,
    marginBottom: 10,
  },

  item: {
    backgroundColor: '#F0F8FA',
    padding: 14,
    borderRadius: 8,
    marginBottom: 8,
  },

  textoItem: {
    fontSize: 16,
  },

  vazio: {
    textAlign: 'center',
    color: '#777',
    margin: 15,
  },

  botaoLimpar: {
    marginTop: 15,
    height: 50,
    borderWidth: 2,
    borderColor: '#176B87',
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },

  textoLimpar: {
    color: '#176B87',
    fontWeight: 'bold',
  },
});


*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 08 / 2026
BIMESTRE: 3º Bimestre
Descrição: Revisão, Contador de Cliques

import React, { useState } from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  StyleSheet,
} from 'react-native';

export default function App() {
  const [cliques, setCliques] = useState(0);

  function contarClique() {
    setCliques(cliques + 1);
  }

  function reiniciar() {
    setCliques(0);
  }

  return (
    <View style={styles.container}>
      <View style={styles.card}>
        <Text style={styles.titulo}>Contador de Cliques</Text>

        <Text style={styles.contador}>
          {cliques}
        </Text>

        <Text style={styles.label}>
          cliques realizados
        </Text>

        <TouchableOpacity
          style={styles.botao}
          onPress={contarClique}
        >
          <Text style={styles.textoBotao}>CLIQUE AQUI</Text>
        </TouchableOpacity>

        <TouchableOpacity
          style={styles.botaoReiniciar}
          onPress={reiniciar}
        >
          <Text style={styles.textoReiniciar}>
            REINICIAR CONTADOR
          </Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EAF4F4',
    justifyContent: 'center',
    padding: 20,
  },

  card: {
    backgroundColor: '#FFF',
    borderRadius: 20,
    padding: 30,
    alignItems: 'center',
    elevation: 5,
  },

  titulo: {
    fontSize: 27,
    fontWeight: 'bold',
    color: '#176B87',
    marginBottom: 25,
    textAlign: 'center',
  },

  contador: {
    fontSize: 60,
    fontWeight: 'bold',
    color: '#176B87',
  },

  label: {
    fontSize: 16,
    color: '#666',
    marginBottom: 25,
  },

  botao: {
    width: '100%',
    height: 52,
    backgroundColor: '#176B87',
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
  },

  textoBotao: {
    color: '#FFF',
    fontSize: 16,
    fontWeight: 'bold',
  },

  botaoReiniciar: {
    width: '100%',
    height: 50,
    borderWidth: 2,
    borderColor: '#176B87',
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 15,
  },

  textoReiniciar: {
    color: '#176B87',
    fontWeight: 'bold',
  },
});

*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 08 / 2026
BIMESTRE: 3º Bimestre
Descrição: Revisão, Escolha de Plano de Internet

import React, { useState } from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  StyleSheet,
} from 'react-native';

export default function App() {
  const [planoSelecionado, setPlanoSelecionado] = useState('');
  const [resultado, setResultado] = useState(null);

  const planos = {
    Básico: {
      valor: 'R$ 49,90',
      beneficios: '100 Mbps, Wi-Fi e suporte online.',
    },

    Intermediário: {
      valor: 'R$ 79,90',
      beneficios: '300 Mbps, Wi-Fi e suporte prioritário.',
    },

    Premium: {
      valor: 'R$ 119,90',
      beneficios: '600 Mbps, Wi-Fi e suporte exclusivo.',
    },
  };

  function contratarPlano() {
    if (planoSelecionado === '') {
      return;
    }

    setResultado({
      nome: planoSelecionado,
      valor: planos[planoSelecionado].valor,
      beneficios: planos[planoSelecionado].beneficios,
    });
  }

  return (
    <View style={styles.container}>
      <View style={styles.card}>
        <Text style={styles.titulo}>
          Planos de Internet
        </Text>

        <Text style={styles.subtitulo}>
          Escolha seu plano:
        </Text>

        {Object.keys(planos).map((plano) => (
          <TouchableOpacity
            key={plano}
            style={[
              styles.opcao,
              planoSelecionado === plano &&
                styles.opcaoSelecionada,
            ]}
            onPress={() => setPlanoSelecionado(plano)}
          >
            <Text
              style={[
                styles.textoOpcao,
                planoSelecionado === plano &&
                  styles.textoSelecionado,
              ]}
            >
              {plano}
            </Text>
          </TouchableOpacity>
        ))}

        <TouchableOpacity
          style={styles.botao}
          onPress={contratarPlano}
        >
          <Text style={styles.textoBotao}>
            VER PLANO
          </Text>
        </TouchableOpacity>

        {resultado && (
          <View style={styles.resultado}>
            <Text style={styles.nomePlano}>
              {resultado.nome}
            </Text>

            <Text style={styles.valor}>
              {resultado.valor} / mês
            </Text>

            <Text style={styles.beneficios}>
              {resultado.beneficios}
            </Text>
          </View>
        )}
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EAF4F4',
    justifyContent: 'center',
    padding: 20,
  },

  card: {
    backgroundColor: '#FFF',
    borderRadius: 20,
    padding: 25,
    elevation: 5,
  },

  titulo: {
    fontSize: 27,
    fontWeight: 'bold',
    color: '#176B87',
    textAlign: 'center',
    marginBottom: 10,
  },

  subtitulo: {
    fontSize: 16,
    marginBottom: 15,
    color: '#555',
  },

  opcao: {
    height: 50,
    borderWidth: 1,
    borderColor: '#B8C8CC',
    borderRadius: 10,
    justifyContent: 'center',
    paddingHorizontal: 15,
    marginBottom: 10,
  },

  opcaoSelecionada: {
    backgroundColor: '#176B87',
  },

  textoOpcao: {
    fontSize: 16,
    fontWeight: 'bold',
    color: '#333',
  },

  textoSelecionado: {
    color: '#FFF',
  },

  botao: {
    backgroundColor: '#176B87',
    height: 52,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 10,
  },

  textoBotao: {
    color: '#FFF',
    fontWeight: 'bold',
    fontSize: 16,
  },

  resultado: {
    marginTop: 20,
    backgroundColor: '#F0F8FA',
    borderRadius: 12,
    padding: 20,
    alignItems: 'center',
  },

  nomePlano: {
    fontSize: 22,
    fontWeight: 'bold',
    color: '#176B87',
  },

  valor: {
    fontSize: 18,
    fontWeight: 'bold',
    marginTop: 8,
  },

  beneficios: {
    textAlign: 'center',
    marginTop: 8,
    color: '#555',
  },
});

*********************************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 12 / 08 / 2026
BIMESTRE: 3º Bimestre
Descrição: Revisão, Aplicativo de Controle Escolar

import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  Alert,
} from 'react-native';

export default function App() {
  const [nota1, setNota1] = useState('');
  const [nota2, setNota2] = useState('');
  const [nota3, setNota3] = useState('');
  const [resultado, setResultado] = useState(null);

  function calcularMedia() {
    const n1 = parseFloat(nota1.replace(',', '.'));
    const n2 = parseFloat(nota2.replace(',', '.'));
    const n3 = parseFloat(nota3.replace(',', '.'));

    if (
      isNaN(n1) ||
      isNaN(n2) ||
      isNaN(n3) ||
      n1 < 0 ||
      n2 < 0 ||
      n3 < 0 ||
      n1 > 10 ||
      n2 > 10 ||
      n3 > 10
    ) {
      Alert.alert(
        'Atenção',
        'Digite três notas válidas entre 0 e 10.'
      );
      return;
    }

    const media = (n1 + n2 + n3) / 3;

    let situacao;

    if (media >= 7) {
      situacao = 'Aprovado';
    } else if (media >= 5) {
      situacao = 'Recuperação';
    } else {
      situacao = 'Reprovado';
    }

    setResultado({
      media: media.toFixed(2),
      situacao: situacao,
    });
  }

  function limparCampos() {
    setNota1('');
    setNota2('');
    setNota3('');
    setResultado(null);
  }

  return (
    <View style={styles.container}>
      <View style={styles.card}>
        <Text style={styles.titulo}>
          Controle Escolar
        </Text>

        <Text style={styles.subtitulo}>
          Digite as três notas do aluno
        </Text>

        <TextInput
          style={styles.input}
          placeholder="Nota 1"
          keyboardType="decimal-pad"
          value={nota1}
          onChangeText={setNota1}
        />

        <TextInput
          style={styles.input}
          placeholder="Nota 2"
          keyboardType="decimal-pad"
          value={nota2}
          onChangeText={setNota2}
        />

        <TextInput
          style={styles.input}
          placeholder="Nota 3"
          keyboardType="decimal-pad"
          value={nota3}
          onChangeText={setNota3}
        />

        <TouchableOpacity
          style={styles.botao}
          onPress={calcularMedia}
        >
          <Text style={styles.textoBotao}>
            CALCULAR MÉDIA
          </Text>
        </TouchableOpacity>

        {resultado && (
          <View style={styles.resultado}>
            <Text style={styles.media}>
              Média: {resultado.media}
            </Text>

            <Text style={styles.situacao}>
              Situação: {resultado.situacao}
            </Text>
          </View>
        )}

        <TouchableOpacity
          style={styles.botaoLimpar}
          onPress={limparCampos}
        >
          <Text style={styles.textoLimpar}>
            LIMPAR CAMPOS
          </Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#EAF4F4',
    justifyContent: 'center',
    padding: 20,
  },

  card: {
    backgroundColor: '#FFF',
    borderRadius: 20,
    padding: 25,
    elevation: 5,
  },

  titulo: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#176B87',
    textAlign: 'center',
    marginBottom: 10,
  },

  subtitulo: {
    textAlign: 'center',
    color: '#666',
    marginBottom: 20,
  },

  input: {
    height: 50,
    borderWidth: 1,
    borderColor: '#B8C8CC',
    borderRadius: 10,
    paddingHorizontal: 15,
    fontSize: 16,
    marginBottom: 12,
  },

  botao: {
    backgroundColor: '#176B87',
    height: 52,
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 5,
  },

  textoBotao: {
    color: '#FFF',
    fontSize: 16,
    fontWeight: 'bold',
  },

  resultado: {
    marginTop: 20,
    backgroundColor: '#F0F8FA',
    borderRadius: 12,
    padding: 20,
    alignItems: 'center',
  },

  media: {
    fontSize: 22,
    fontWeight: 'bold',
    color: '#176B87',
  },

  situacao: {
    fontSize: 18,
    fontWeight: 'bold',
    marginTop: 8,
  },

  botaoLimpar: {
    height: 50,
    borderWidth: 2,
    borderColor: '#176B87',
    borderRadius: 10,
    justifyContent: 'center',
    alignItems: 'center',
    marginTop: 15,
  },

  textoLimpar: {
    color: '#176B87',
    fontWeight: 'bold',
  },
});

***********************************************************
INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Rakely da Silva de Souza
DADOS: 26 / 08/ 2026
BIMESTRE: 3º Bimestre
Descrição: ecofeira am

import React, { useState } from 'react';
import { View, Text, TextInput, TouchableOpacity, StyleSheet, ScrollView, Alert } from 'react-native';

export default function App() {
const [tela, setTela] = useState(1);

const [usuario, setUsuario] = useState('');
const [senha, setSenha] = useState('');
const [perfil, setPerfil] = useState('Comprador');

// Agora o carrinho é uma lista [] vazia no início
const [carrinho, setCarrinho] = useState([]);
const [frete, setFrete] = useState(10);
const [cidade, setCidade] = useState('Manaus');

function realizarLogin() {
if (usuario.trim() === '' || senha.trim() === '') {
Alert.alert("Erro", "Por favor, preencha o usuário e a senha!");
return;
}

if (senha !== '1234') {
Alert.alert("Erro", "Senha incorreta! Use '1234'.");
return;
}

Alert.alert("Bem-vindo!", `Login realizado com sucesso como ${perfil}.`);
setTela(2);
}

// Recebe o nome e o preço do produto clicado e salva na lista
function adicionarItem(nomeProduto, precoProduto) {
setCarrinho([...carrinho, { nome: nomeProduto, preco: precoProduto }]);
Alert.alert("Sucesso", `${nomeProduto} adicionado!`);
}

function escolherManaus() {
setCidade('Manaus');
setFrete(10);
}

function escolherIranduba() {
setCidade('Iranduba');
setFrete(15);
}

function voltarTela() {
if (tela === 3) setTela(2);
else if (tela === 2) setTela(1);
}

function finalizarPedido() {
if (carrinho.length === 0) {
Alert.alert("Aviso", "Seu carrinho está vazio!");
return;
}
Alert.alert("Pedido Confirmado", `Pedido enviado pelo perfil: ${perfil}`);
setCarrinho([]); // Limpa a lista após finalizar
setTela(1);
}

// Cálculos matemáticos dos totais
const totalProdutos = carrinho.reduce((soma, item) => soma + item.preco, 0);
const valorTotalFinal = totalProdutos + (carrinho.length > 0 ? frete : 0);

// --- TELA 1 ---
if (tela === 1) {
return (
<View style={styles.containerCenter}>
<Text style={styles.titulo}>🌱 EcoFeira AM</Text>
<Text style={styles.subtitulo}>Acesso ao Sistema</Text>

<View style={styles.formContainer}>
<Text style={styles.label}>Usuário / E-mail:</Text>
<TextInput
style={styles.input}
placeholder="Digite seu usuário"
value={usuario}
onChangeText={setUsuario}
autoCapitalize="none"
/>

<Text style={styles.label}>Senha (use 1234):</Text>
<TextInput
style={styles.input}
placeholder="Digite sua senha"
secureTextEntry={true}
value={senha}
onChangeText={setSenha}
/>

<Text style={styles.label}>Perfil de Acesso:</Text>
<View style={styles.linhaBotoes}>
<TouchableOpacity
style={[styles.botaoOpcao, perfil === 'Comprador' && styles.opcaoAtiva]}
onPress={() => setPerfil('Comprador')}
>
<Text style={styles.textoOpcao}>Comprador</Text>
</TouchableOpacity>

<TouchableOpacity
style={[styles.botaoOpcao, perfil === 'Produtor' && styles.opcaoAtiva]}
onPress={() => setPerfil('Produtor')}
>
<Text style={styles.textoOpcao}>Produtor</Text>
</TouchableOpacity>
</View>

<TouchableOpacity style={styles.botaoPrincipal} onPress={realizarLogin}>
<Text style={styles.textoBotao}>ENTRAR</Text>
</TouchableOpacity>
</View>
</View>
);
}

// --- TELA 2 ---
if (tela === 2) {
return (
<View style={styles.container}>
<Text style={styles.titulo}>Produtos Regionais</Text>
<Text style={styles.subtituloHeader}>Usuário: {usuario} ({perfil})</Text>

<ScrollView style={styles.scroll}>
<View style={styles.card}>
<Text style={styles.nomeItem}>Açaí Regional (1 Litro)</Text>
<Text style={styles.origem}>Origem: Iranduba</Text>
<Text style={styles.preco}>R$ 20,00</Text>
<TouchableOpacity style={styles.botaoAdd} onPress={() => adicionarItem('Açaí Regional (1 Litro)', 20)}>
<Text style={styles.textoBotao}>+ Adicionar</Text>
</TouchableOpacity>
</View>

<View style={styles.card}>
<Text style={styles.nomeItem}>Farinha de Uarini (1kg)</Text>
<Text style={styles.origem}>Origem: Tefé</Text>
<Text style={styles.preco}>R$ 15,00</Text>
<TouchableOpacity style={styles.botaoAdd} onPress={() => adicionarItem('Farinha de Uarini (1kg)', 15)}>
<Text style={styles.textoBotao}>+ Adicionar</Text>
</TouchableOpacity>
</View>

<View style={styles.card}>
<Text style={styles.nomeItem}>Tucumã Descascado (500g)</Text>
<Text style={styles.origem}>Origem: Iranduba</Text>
<Text style={styles.preco}>R$ 25,00</Text>
<TouchableOpacity style={styles.botaoAdd} onPress={() => adicionarItem('Tucumã Descascado (500g)', 25)}>
<Text style={styles.textoBotao}>+ Adicionar</Text>
</TouchableOpacity>
</View>

<View style={styles.card}>
<Text style={styles.nomeItem}>Peixe Tambaqui Fresco (kg)</Text>
<Text style={styles.origem}>Origem: Manacapuru</Text>
<Text style={styles.preco}>R$ 30,00</Text>
<TouchableOpacity style={styles.botaoAdd} onPress={() => adicionarItem('Peixe Tambaqui Fresco (kg)', 30)}>
<Text style={styles.textoBotao}>+ Adicionar</Text>
</TouchableOpacity>
</View>
</ScrollView>

<View style={styles.rodape}>
<TouchableOpacity style={styles.botaoPrincipal} onPress={() => setTela(3)}>
<Text style={styles.textoBotao}>VER CARRINHO ({carrinho.length})</Text>
</TouchableOpacity>

<TouchableOpacity style={styles.botaoVoltar} onPress={voltarTela}>
<Text style={styles.textoVoltar}>← Sair / Voltar para Login</Text>
</TouchableOpacity>
</View>
</View>
);
}

// --- TELA 3 ---
return (
<View style={styles.container}>
<Text style={styles.titulo}>Seu Carrinho</Text>

<ScrollView style={styles.scroll}>
<Text style={styles.subtitulo}>Selecione a Cidade de Entrega:</Text>
<View style={styles.linhaBotoes}>
<TouchableOpacity
style={[styles.botaoOpcao, cidade === 'Manaus' && styles.opcaoAtiva]}
onPress={escolherManaus}
>
<Text style={styles.textoOpcao}>Manaus (R$10)</Text>
</TouchableOpacity>

<TouchableOpacity
style={[styles.botaoOpcao, cidade === 'Iranduba' && styles.opcaoAtiva]}
onPress={escolherIranduba}
>
<Text style={styles.textoOpcao}>Iranduba (R$15)</Text>
</TouchableOpacity>
</View>

<View style={styles.cardResumo}>
<Text style={styles.nomeItem}>Resumo do Pedido</Text>
<Text style={styles.textoResumo}>Cliente: {usuario}</Text>

<Text style={[styles.textoResumo, {marginTop: 15, fontWeight: 'bold'}]}>Itens Escolhidos:</Text>
{carrinho.length === 0 ? (
<Text style={styles.textoResumo}>Nenhum produto adicionado.</Text>
) : (
carrinho.map((produto, index) => (
<Text key={index} style={styles.textoItemLista}>
• {produto.nome} - R$ {produto.preco},00
</Text>
))
)}

<View style={styles.linhaTotais}>
<Text style={styles.textoValores}>Subtotal dos Produtos:</Text>
<Text style={styles.textoValores}>R$ {totalProdutos},00</Text>
</View>

<View style={styles.linhaTotais}>
<Text style={styles.textoValores}>Frete ({cidade}):</Text>
<Text style={styles.textoValores}>R$ {carrinho.length > 0 ? frete : 0},00</Text>
</View>

<View style={[styles.linhaTotais, {borderTopWidth: 1, borderColor: '#ccc', paddingTop: 10, marginTop: 10}]}>
<Text style={styles.textoTotalFinal}>VALOR TOTAL:</Text>
<Text style={styles.textoTotalFinal}>R$ {valorTotalFinal},00</Text>
</View>

</View>
</ScrollView>

<View style={styles.rodape}>
<TouchableOpacity style={styles.botaoPrincipal} onPress={finalizarPedido}>
<Text style={styles.textoBotao}>CONFIRMAR PEDIDO</Text>
</TouchableOpacity>

<TouchableOpacity style={styles.botaoVoltar} onPress={voltarTela}>
<Text style={styles.textoVoltar}>← Voltar para Produtos</Text>
</TouchableOpacity>
</View>
</View>
);
}

const styles = StyleSheet.create({
container: { flex: 1, backgroundColor: '#f5f5f5', padding: 20, paddingTop: 40 },
containerCenter: { flex: 1, backgroundColor: '#f5f5f5', padding: 20, justifyContent: 'center' },
formContainer: { backgroundColor: '#fff', padding: 20, borderRadius: 8, borderWidth: 1, borderColor: '#ddd' },
scroll: { flex: 1, marginBottom: 10 },
titulo: { fontSize: 24, fontWeight: 'bold', color: '#2e7d32', textAlign: 'center', marginBottom: 5 },
subtitulo: { fontSize: 16, color: '#555', textAlign: 'center', marginBottom: 15 },
subtituloHeader: { fontSize: 13, color: '#666', textAlign: 'center', marginBottom: 10 },
label: { fontSize: 14, fontWeight: 'bold', marginTop: 10, marginBottom: 5, color: '#333' },
input: { borderWidth: 1, borderColor: '#ccc', borderRadius: 6, padding: 10, backgroundColor: '#fff' },
card: { backgroundColor: '#fff', padding: 15, borderRadius: 8, marginBottom: 12, borderWidth: 1, borderColor: '#ddd' },
cardResumo: { backgroundColor: '#fff', padding: 20, borderRadius: 8, marginTop: 15, borderWidth: 1, borderColor: '#ddd' },
nomeItem: { fontSize: 18, fontWeight: 'bold', color: '#333' },
origem: { fontSize: 12, color: '#777', marginVertical: 2 },
preco: { fontSize: 16, fontWeight: 'bold', color: '#2e7d32', marginVertical: 4 },
textoResumo: { fontSize: 14, marginTop: 4, color: '#444' },
textoItemLista: { fontSize: 14, color: '#2e7d32', marginLeft: 10, marginTop: 4 },
linhaTotais: { flexDirection: 'row', justifyContent: 'space-between', marginTop: 10 },
textoValores: { fontSize: 15, color: '#333' },
textoTotalFinal: { fontSize: 18, fontWeight: 'bold', color: '#d32f2f' },
linhaBotoes: { flexDirection: 'row', justifyContent: 'space-between', marginVertical: 10 },
botaoOpcao: { flex: 1, padding: 10, borderWidth: 1, borderColor: '#ccc', borderRadius: 6, alignItems: 'center', marginHorizontal: 4, backgroundColor: '#fff' },
opcaoAtiva: { backgroundColor: '#c8e6c9', borderColor: '#2e7d32' },
textoOpcao: { fontWeight: 'bold', fontSize: 13 },
botaoAdd: { backgroundColor: '#2e7d32', padding: 10, borderRadius: 6, alignItems: 'center', marginTop: 6 },
rodape: { paddingTop: 10, borderTopWidth: 1, borderColor: '#e0e0e0' },
botaoPrincipal: { backgroundColor: '#2e7d32', padding: 15, borderRadius: 8, alignItems: 'center', marginTop: 10 },
textoBotao: { color: '#fff', fontWeight: 'bold', fontSize: 15 },
botaoVoltar: { padding: 12, alignItems: 'center' },
textoVoltar: { color: '#c62828', fontWeight: 'bold', fontSize: 15 }
});

