**********************************************************************/
INSTITUTO FEDERAL DO AMAZONAS – CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  28/ 04 /2026
Bimestre: 2º Bimestre
Descrição: Modifique o app.
Grupo A
**********************************************************************/

import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet, Alert, StatusBar } from 'react-native';

export default function App() {

  function handlePress() {
    Alert.alert("Você iniciou o app! Bons estudos 📚");
  }

  return (
    <View style={styles.container}>
      
      <StatusBar barStyle="light-content" backgroundColor="#0F172A" />

      {/* Título */}
      <Text style={styles.title}>Seja bem-vindo, aluno! </Text>

      {/* Subtítulo */}
      <Text style={styles.subtitle}>
        Pronto para começar sua jornada?
      </Text>

      {/* Botão */}
      <TouchableOpacity style={styles.button} onPress={handlePress}>
        <Text style={styles.buttonText}>Iniciar</Text>
      </TouchableOpacity>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#0F172A',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 20,
  },

  title: {
    fontSize: 34,
    fontWeight: 'bold',
    color: '#F8FAFC',
    marginBottom: 8,
  },

  subtitle: {
    fontSize: 16,
    color: '#94A3B8',
    marginBottom: 50,
  },

  button: {
    backgroundColor: '#22C55E',
    paddingVertical: 16,
    paddingHorizontal: 70,
    borderRadius: 12,
    elevation: 6,
  },

  buttonText: {
    color: '#0F172A',
    fontSize: 18,
    fontWeight: 'bold',
  },
});




**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  28/ 04 /2026
Bimestre: 2º Bimestre
Descrição da Atividade: Nesta atividade foi desenvolvido um aplicativo simples utilizando React Native, com o objetivo de compreender a estrutura básica de um app mobile.
O aplicativo apresenta uma tela inicial com uma mensagem de boas-vindas e um botão interativo que, ao ser pressionado, exibe um alerta na tela.
Também foram aplicados conceitos de estilização com StyleSheet, permitindo a personalização de cores e textos.
A atividade contribuiu para o aprendizado dos principais componentes, como View, Text, Button e Alert, além de reforçar a lógica de programação.

**********************************************************************/
Prática: Sua Primeira Tela
Agora é a sua vez! Crie uma tela simples usando Snack (React Native) ou App
Inventor.


import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet, Alert, StatusBar } from 'react-native';

export default function App() {

  function handlePress() {
    Alert.alert("Você clicou em Entrar!");
  }

  return (
    <View style={styles.container}>
      
      <StatusBar barStyle="light-content" backgroundColor="#4A90E2" />

      {/* Título */}
      <Text style={styles.title}>Bem-vindo </Text>

      {/* Subtítulo */}
      <Text style={styles.subtitle}>
        Seu app começa aqui
      </Text>

      {/* Botão */}
      <TouchableOpacity style={styles.button} onPress={handlePress}>
        <Text style={styles.buttonText}>Entrar</Text>
      </TouchableOpacity>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#4A90E2',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 20,
  },

  title: {
    fontSize: 32,
    fontWeight: 'bold',
    color: '#fff',
    marginBottom: 10,
  },

  subtitle: {
    fontSize: 16,
    color: '#E0E0E0',
    marginBottom: 40,
  },

  button: {
    backgroundColor: '#fff',
    paddingVertical: 15,
    paddingHorizontal: 60,
    borderRadius: 30,
    
    // sombra (Android + iOS)
    elevation: 5,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.3,
    shadowRadius: 4,
  },

  buttonText: {
    color: '#4A90E2',
    fontSize: 18,
    fontWeight: 'bold',
  },
});

**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  05/ 05 /2026
Bimestre: 2º Bimestre
Descrição da Atividade: Formulários, Dados e Conexão com a Internet/ parte 1
**********************************************************************/
Boas Práticas em Formulários
adicionar cor de fundo

import React, { useState } from 'react'; // Importa o React e o useState para guardar dados digitados

import {
  View, // Cria áreas na tela
  Text, // Exibe textos
  TextInput, // Campo para digitar
  TouchableOpacity, // Botão clicável
  StyleSheet, // Cria estilos
  Alert // Mostra notificações
} from 'react-native';

export default function App() { // Função principal do aplicativo

  // Variável para guardar o nome digitado
  const [nome, setNome] = useState('');

  // Variável para guardar o email digitado
  const [email, setEmail] = useState('');

  // Variável para guardar o CPF digitado
  const [cpf, setCpf] = useState('');

  // Função que verifica se os campos estão preenchidos
  function validarFormulario() {

    // Se nome, email ou cpf estiver vazio
    if (nome === '' || email === '' || cpf === '') {

      // Mostra alerta avisando
      Alert.alert(
        'Campos obrigatórios',
        'Por favor, preencha todas as informações.'
      );

    } else {

      // Se tudo estiver preenchido, mostra sucesso
      Alert.alert(
        'Sucesso!',
        'Cadastro realizado com sucesso.'
      );
    }
  }

  // O que aparece na tela
  return (

    // Área principal
    <View style={styles.container}>

      {/* Título */}
      <Text style={styles.titulo}>Inscreva-se</Text>

      {/* Campo Nome */}
      <TextInput
        style={styles.input} // Aplica estilo
        placeholder="Nome" // Texto dentro do campo
        value={nome} // Liga ao estado nome
        onChangeText={setNome} // Atualiza o valor digitado
      />

      {/* Campo Email */}
      <TextInput
        style={styles.input}
        placeholder="E-mail"
        value={email}
        onChangeText={setEmail}
        keyboardType="email-address" // Teclado para email
      />

      {/* Campo CPF */}
      <TextInput
        style={styles.input}
        placeholder="CPF"
        value={cpf}
        onChangeText={setCpf}
        keyboardType="numeric" // Teclado numérico
      />

      {/* Botão */}
      <TouchableOpacity
        style={styles.botao}
        onPress={validarFormulario} // Quando clicar chama a função
      >

        <Text style={styles.textoBotao}>
          Inscrever-se
        </Text>

      </TouchableOpacity>

    </View>
  );
}

// Área de estilos
const styles = StyleSheet.create({

  // Estilo da tela principal
  container: {
    flex: 1, // Ocupa toda a tela
    justifyContent: 'center', // Centraliza verticalmente
    padding: 20, // Espaço interno
    backgroundColor: '#fff' // Fundo branco
  },

  // Estilo do título
  titulo: {
    fontSize: 28, // Tamanho da letra
    fontWeight: 'bold', // Negrito
    textAlign: 'center', // Centralizado
    marginBottom: 30 // Espaço abaixo
  },

  // Estilo dos campos
  input: {
    borderWidth: 1, // Espessura da borda
    borderColor: '#ccc', // Cor da borda
    borderRadius: 8, // Borda arredondada
    padding: 12, // Espaço interno
    marginBottom: 15 // Espaço abaixo
  },

  // Estilo do botão
  botao: {
    backgroundColor: '#7B2CF5', // Cor roxa
    padding: 15,
    borderRadius: 8
  },

  // Estilo do texto do botão
  textoBotao: {
    color: '#fff', // Branco
    textAlign: 'center',
    fontWeight: 'bold'
  }

});




**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  05/ 05 /2026
Bimestre: 2º Bimestre
Descrição da Atividade: Formulários, Dados e Conexão com a Internet/ parte 2
**********************************************************************/
Boas Práticas em Formulários
adicionar cor de fundo e imagem


import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  TouchableOpacity,
  StyleSheet,
  Alert,
  Image
} from 'react-native';

export default function App() {

  const [nome, setNome] = useState('');
  const [email, setEmail] = useState('');
  const [cpf, setCpf] = useState('');

  function validarFormulario() {
    if (nome === '' || email === '' || cpf === '') {
      Alert.alert(
        'Campos obrigatórios',
        'Por favor, preencha todas as informações.'
      );
    } else {
      Alert.alert(
        'Sucesso!',
        'Cadastro realizado com sucesso.'
      );
    }
  }

  return (
    <View style={styles.container}>

      {/* Logo do IF Goiano */}
      <Image
        source={ require ('./assets/logoif.png')}
        style={styles.logo}
      />

      <Text style={styles.titulo}>Inscreva-se</Text>

      <TextInput
        style={styles.input}
        placeholder="Nome"
        value={nome}
        onChangeText={setNome}
      />

      <TextInput
        style={styles.input}
        placeholder="E-mail"
        value={email}
        onChangeText={setEmail}
        keyboardType="email-address"
      />

      <TextInput
        style={styles.input}
        placeholder="CPF"
        value={cpf}
        onChangeText={setCpf}
        keyboardType="numeric"
      />

      <TouchableOpacity
        style={styles.botao}
        onPress={validarFormulario}
      >
        <Text style={styles.textoBotao}>
          Inscrever-se
        </Text>
      </TouchableOpacity>

    </View>
  );
}

const styles = StyleSheet.create({

  container: {
    flex: 1,
    justifyContent: 'center',
    padding: 20,
    backgroundColor: '#0B7A3E'
  },

  logo: {
    width: 170,
    height: 90,
    resizeMode: 'contain',
    alignSelf: 'center',
    marginBottom: 20
  },

  titulo: {
    fontSize: 28,
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 30,
    color: '#fff'
  },

  input: {
    backgroundColor: '#fff',
    borderWidth: 1,
    borderColor: '#ccc',
    borderRadius: 8,
    padding: 12,
    marginBottom: 15
  },

  botao: {
    backgroundColor: '#d60000',
    padding: 15,
    borderRadius: 8
  },

  textoBotao: {
    color: '#fff',
    textAlign: 'center',
    fontWeight: 'bold'
  }

});



**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  12/ 05 /2026
Bimestre: 2º Bimestre
Descrição: arvore de natal

**********************************************************************/

import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>

      <Text style={styles.estrela}>⭐</Text>

      {/* Linha 1 */}
      <View style={styles.linha}>
        <Text style={[styles.caixa, { backgroundColor: '#33FF57' }]}>
          A
        </Text>
      </View>

      {/* Linha 2 */}
      <View style={styles.linha}>
        <Text style={[styles.caixa, { backgroundColor: '#F333FF' }]}>
          B
        </Text>

        <Text style={[styles.caixa, { backgroundColor: '#3357FF' }]}>
          C
        </Text>
      </View>

      {/* Linha 3 */}
      <View style={styles.linha}>
        <Text style={[styles.caixa, { backgroundColor: '#FF5733' }]}>
          D
        </Text>

        <Text style={[styles.caixa, { backgroundColor: '#FFB833' }]}>
          E
        </Text>

        <Text style={[styles.caixa, { backgroundColor: '#33AAFF' }]}>
          F
        </Text>
      </View>

      {/* Tronco */}
      <View style={styles.tronco} />

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f0f0f0',
  },

  estrela: {
    fontSize: 40,
    marginBottom: 10,
  },

  linha: {
    flexDirection: 'row',
    justifyContent: 'center',
  },

  caixa: {
    width: 70,
    height: 70,
    margin: 3,
    color: 'white',
    fontSize: 28,
    fontWeight: 'bold',
    textAlign: 'center',
    paddingTop: 15,
  },

  tronco: {
    width: 60,
    height: 80,
    backgroundColor: '#8B4513',
    marginTop: 5,
  },
});

**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  12/ 05 /2026
Bimestre: 2º Bimestre
Descrição: Nesta atividade foi criada uma tela simples no React Native contendo um título, subtítulo, 
três caixas alinhadas horizontalmente e um botão. Foram utilizados componentes básicos como View, Text e TouchableOpacity,
além de estilizações com StyleSheet para centralizar e organizar os elementos da interface.

**********************************************************************/
import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>

      {/* Título */}
      <Text style={styles.titulo}>Meu Aplicativo</Text>

      {/* Subtítulo */}
      <Text style={styles.subtitulo}>Aula de Layouts</Text>

      {/* Caixas alinhadas */}
      <View style={styles.linha}>
        <View style={styles.caixa}>
          <Text style={styles.textoCaixa}>A</Text>
        </View>

        <View style={styles.caixa}>
          <Text style={styles.textoCaixa}>B</Text>
        </View>

        <View style={styles.caixa}>
          <Text style={styles.textoCaixa}>C</Text>
        </View>
      </View>

      {/* Botão */}
      <TouchableOpacity style={styles.botao}>
        <Text style={styles.textoBotao}>ENTRAR</Text>
      </TouchableOpacity>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#111',
    justifyContent: 'center',
    alignItems: 'center',
  },

  titulo: {
    fontSize: 28,
    color: '#fff',
    fontWeight: 'bold',
  },

  subtitulo: {
    fontSize: 18,
    color: '#ccc',
    marginBottom: 30,
  },

  linha: {
    flexDirection: 'row',
    marginBottom: 30,
  },

  caixa: {
    width: 60,
    height: 60,
    backgroundColor: '#333',
    borderWidth: 2,
    borderColor: '#fff',
    justifyContent: 'center',
    alignItems: 'center',
    marginHorizontal: 10,
  },

  textoCaixa: {
    color: '#fff',
    fontSize: 20,
  },

  botao: {
    backgroundColor: '#fff',
    paddingVertical: 12,
    paddingHorizontal: 30,
    borderRadius: 10,
  },

  textoBotao: {
    fontWeight: 'bold',
  },
});

**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  12/ 05 /2026
Bimestre: 2º Bimestre
Descrição: Esta atividade teve como objetivo personalizar a interface do aplicativo utilizando imagem, 
ícones, cores personalizadas e bordas arredondadas. 
Foram aplicados recursos visuais do React Native para deixar o aplicativo mais moderno,
organizado e visualmente agradável.

**********************************************************************/

import React from 'react';
import { View, Text, Image, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>

      {/* Imagem */}
      <Image
        source={{
          uri: 'https://reactnative.dev/img/tiny_logo.png',
        }}
        style={styles.imagem}
      />

      {/* Ícones usando emojis */}
      <Text style={styles.icone}>⭐ 🎨 🚀</Text>

      {/* Caixa personalizada */}
      <View style={styles.caixa}>
        <Text style={styles.texto}>Caixa com bordas arredondadas</Text>
      </View>

    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#1e1e1e',
    justifyContent: 'center',
    alignItems: 'center',
  },

  imagem: {
    width: 100,
    height: 100,
    marginBottom: 20,
  },

  icone: {
    fontSize: 35,
    marginBottom: 20,
  },

  caixa: {
    backgroundColor: '#4CAF50',
    padding: 20,
    borderRadius: 20,
  },

  texto: {
    color: '#fff',
    fontSize: 16,
    textAlign: 'center',
  },
});






**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  12/ 05 /2026
Bimestre: 2º Bimestre
Descrição: Esta atividade teve como objetivo modificar o projeto para adiconar mais 4 cards,mudar cores, adicionar ícones,inserir uma imagem no topo e criar um botão abaixo dos cards.
**********************************************************************/

import React from 'react';
import { View, Text, StyleSheet, ScrollView, Image, TouchableOpacity } from 'react-native';
import { MaterialCommunityIcons } from '@expo/vector-icons';


export default function App() {
 
  const cards = [
    { id: 1, title: 'Card 1', icon: 'rocket' },
    { id: 2, title: 'Card 2', icon: 'heart' },
    { id: 3, title: 'Card 3', icon: 'star' },
    { id: 4, title: 'Card 4', icon: 'camera' },
    { id: 5, title: 'Card 5', icon: 'bell' },
    { id: 6, title: 'Card 6', icon: 'check-circle' },
  ];


  return (
    <ScrollView style={styles.container}>
     
      <Image
        source={{ uri: 'https://picsum.photos' }}
        style={styles.headerImage}
      />


      <Text style={styles.title}>Meu Layout Responsivo</Text>


      {/* Container dos Cards */}
      <View style={styles.cardContainer}>
        {cards.map((card) => (
          <View key={card.id} style={styles.card}>
            {/* 3. Adicionar ícones */}
            <MaterialCommunityIcons name={card.icon} size={50} color="#4B0C82" />
            <Text style={styles.cardText}>{card.title}</Text>
          </View>
        ))}
      </View>
 
      {/* 5. Botão abaixo dos cards */}
      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>Confirmar Atividade</Text>
      </TouchableOpacity>
    </ScrollView>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#7DC9BD', // Cor de fundo suave
  },
  headerImage: {
    width: '100%',
    height: 140,
  },
  title: {
    fontSize: 22,
    fontWeight: 'bold',
    textAlign: 'center',
    marginVertical: 20,
    color: '#333',
  },
  cardContainer: {
    flexDirection: 'row',
    flexWrap: 'wrap', // Faz os cards "quebrarem" para a linha de baixo
    justifyContent: 'space-around',
    paddingHorizontal: 10,
  },
  card: {
    // 1 e 2. Mais cards e novas cores
    backgroundColor: '#6C63FF',
    width: '45%', // Garante 2 cards por linha
    height: 120,
    borderRadius: 15,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 15,
    // Sombra para o card
    elevation: 5,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 2 },
    shadowOpacity: 0.2,
  },
  cardText: {
    color: '#fff',
    marginTop: 10,
    fontWeight: 'bold',
  },
  button: {
    backgroundColor: '#2ECC71',
    margin: 20,
    padding: 15,
    borderRadius: 10,
    alignItems: 'center',
  },
  buttonText: {
    color: '#fff',
    fontSize: 18,
    fontWeight: 'bold',
  },
});





**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  19/ 05 /2026
Bimestre: 2º Bimestre
Descrição: Nesta atividade vamos modificar o nosso primeiro codigo, adicionando dois titulos, dois subtitulos e dois botoes
**********************************************************************/
import React from 'react';
import { View, Text, TouchableOpacity, StyleSheet, Alert, StatusBar } from 'react-native';


export default function App() {


  function handlePress() {
    Alert.alert("Você iniciou o app! Bons estudos 📚");
  }


  return (
    <View style={styles.container}>
     
      <StatusBar barStyle="light-content" backgroundColor="#0F172A" />


      {/* Título */}
      <Text style={styles.title}>Seja Bem-Vindo, Aluno! </Text>
      <Text style={styles.title}>Vamos Estudar?  </Text>


      {/* Subtítulo */}
      <Text style={styles.subtitle}>
        Pronto para começar sua jornada?
      </Text>
      <Text style={styles.subtitle}>
        Vamos rever o conteudo?
      </Text>


      {/* Botão */}
      <TouchableOpacity style={styles.button} onPress={handlePress}>
        <Text style={styles.buttonText}>Iniciar</Text>
      </TouchableOpacity>


      <Text style={styles.title}>
      </Text>


      <TouchableOpacity style={styles.button} onPress={handlePress}>
        <Text style={styles.buttonText}>Reiniciar</Text>
      </TouchableOpacity>




    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#030063',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 20,
  },


  title: {
    fontSize: 34,
    fontWeight: 'bold',
    color: '#F8FAFC',
    marginBottom: 8,
  },


  subtitle: {
    fontSize: 16,
    color: '#FFFCFC',
    marginBottom: 50,
  },


  button: {
    backgroundColor: '#5C5CBF',
    paddingVertical: 16,
    paddingHorizontal: 70,
    borderRadius: 12,
    elevation: 6,
  },


  buttonText: {
    color: '#FFFCFC',
    fontSize: 18,
    fontWeight: 'bold',
  },
});






**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  19/ 05 /2026
Bimestre: 2º Bimestre
Descrição: Nesta atividade vamos modificar as fontes.
**********************************************************************/
import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      <Text style={styles.tituloSans}>ANA MOSER</Text>
      <Text style={styles.tituloSerif}>ANA MOSER</Text>
      <Text style={styles.tituloMono}>ANA MOSER</Text>
      <Text style={styles.tituloMonof}>ANA MOSER</Text>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#f5f5f5',
    padding: 20,
  },
  tituloSans: {
    fontSize: 30,
    fontWeight: 'bold',
    fontStyle: 'italic', // Aplica o itálico corretamente
    fontFamily: 'sans-serif', // Fonte nativa limpa
    color: '#5A1066',
    marginBottom: 60,
  },
  tituloSerif: {
    fontSize: 40,
    fontWeight: 'bold',
    fontStyle: 'italic', // Aplica o itálico corretamente
    fontFamily: 'serif', // Fonte clássica com serifas
    color: '#D615B8',
    marginBottom: 60,
  },
  tituloMono: {
    fontSize: 50,
    fontWeight: 'bold',
    fontFamily: 'monospace', // Fonte estilo código
    color: '#1EC3E6',
    marginBottom: 60,
  },
  tituloMonof: {
    fontSize: 35,
    fontWeight: 'normal',
    fontFamily: 'monospace', // Adicionado para o quarto elemento do seu layout
    color: '#333333',
    marginBottom: 60,
  },
});


**********************************************************************/

INSTITUTO FEDERAL DO AMAZONAS – IFAM CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  02/ 06 /2026
Bimestre: 2º Bimestre
Descrição: NESTA ATIVIDADE MUDAMOS O PROJETO INTERMEDIARIO, SENDO PEDIDO
Entregáveis
1 Código fonte comentado e b e m estruturado
2 Aplicação funcionando no Expo Snack
3 Documentação explicando as funcionalidades
4 Apresentação do projeto em sala de aula
Avaliação: Funcionalidade (0,4%), Códi go (0,3%),
Documentação (0,2%), Apresentação (0,1%)
**********************************************************************/


import React, { useState } from 'react';
import {
  StyleSheet,
  Text,
  View,
  TextInput,
  TouchableOpacity,
  Alert,
  ScrollView
} from 'react-native';


export default function App() {
  // --- ESTADOS EXISTENTES (Mantidos) ---
  const [produtos, setProdutos] = useState([]);
  const [nome, setNome] = useState('');
  const [preco, setPreco] = useState('');
  const [categoria, setCategoria] = useState('Comésticos');
  const [descricao, setDescricao] = useState('');
  const [editandoId, setEditandoId] = useState(null);


  // --- NOVOS ESTADOS (Adicionados) ---
  const [sku, setSku] = useState('');
  const [estoque, setEstoque] = useState('');


  // --- CÁLCULOS AUTOMÁTICOS PARA O DASHBOARD ---
  const totalItensEstoque = produtos.reduce((acc, curr) => acc + parseInt(curr.estoque || 0), 0);
  const valorTotalEstoque = produtos.reduce((acc, curr) => acc + (parseFloat(curr.preco) * parseInt(curr.estoque || 0)), 0);


  // --- FUNÇÕES DE MANIPULAÇÃO ---


  // Salvar ou Atualizar Produto
  const salvarProduto = () => {
    // Validação de dados (Todos os campos se tornaram obrigatórios)
    if (!nome.trim() || !preco.trim() || !descricao.trim() || !sku.trim() || !estoque.trim()) {
      Alert.alert('Campos Incompletos', 'Por favor, preencha todos os campos do formulário.');
      return;
    }


    // Validação do preço
    const precoNum = parseFloat(preco.replace(',', '.'));
    if (isNaN(precoNum) || precoNum <= 0) {
      Alert.alert('Preço Inválido', 'Por favor, insira um preço maior que zero.');
      return;
    }


    // Nova Validação: Estoque (Apenas números inteiros positivos)
    const estoqueNum = parseInt(estoque);
    if (isNaN(estoqueNum) || estoqueNum < 0) {
      Alert.alert('Estoque Inválido', 'A quantidade em estoque deve ser um número maior ou igual a zero.');
      return;
    }


    if (editandoId) {
      // Modo Edição: Atualiza o produto existente mantendo a estrutura
      setProdutos(produtos.map(prod =>
        prod.id === editandoId
          ? {
              ...prod,
              nome,
              preco: precoNum.toFixed(2),
              categoria,
              descricao,
              sku: sku.toUpperCase().trim(),
              estoque: estoqueNum.toString()
            }
          : prod
      ));
      setEditandoId(null);
      Alert.alert('Sucesso', 'Produto atualizado com sucesso!');
    } else {
      // Modo Cadastro: Cria um novo produto com as novas informações inclusas
      const novoProduto = {
        id: Date.now().toString(),
        nome,
        preco: precoNum.toFixed(2),
        categoria,
        descricao,
        sku: sku.toUpperCase().trim(),
        estoque: estoqueNum.toString()
      };
      setProdutos([...produtos, novoProduto]);
      Alert.alert('Sucesso', 'Produto cadastrado com sucesso!');
    }


    limparCampos();
  };


  // Preparar campos para edição (Atualizado com os novos campos)
  const iniciarEdicao = (produto) => {
    setEditandoId(produto.id);
    setNome(produto.nome);
    setPreco(produto.preco.toString());
    setCategoria(produto.categoria);
    setDescricao(produto.descricao);
    setSku(produto.sku || '');
    setEstoque(produto.estoque ? produto.estoque.toString() : '');
  };


  // Deletar Produto
  const deletarProduto = (id) => {
    Alert.alert(
      'Excluir Produto',
      'Tem certeza que deseja deletar este produto?',
      [
        { text: 'Cancelar', style: 'cancel' },
        {
          text: 'Excluir',
          style: 'destructive',
          onPress: () => {
            setProdutos(produtos.filter(prod => prod.id !== id));
            if (editandoId === id) limparCampos();
          }
        }
      ]
    );
  };


  // Limpar o formulário (Atualizado com os novos campos)
  const limparCampos = () => {
    setNome('');
    setPreco('');
    setCategoria('Comésticos');
    setDescricao('');
    setSku('');
    setEstoque('');
    setEditandoId(null);
  };


  return (
    <ScrollView style={styles.container}>
      <Text style={styles.tituloPrincipal}>Produtos Ivone 💄</Text>
       <Text style={styles.subtitle}>
        Comésticos e maquiagens ✨
      </Text>

     
      {/* CARD DE DASHBOARD / RESUMO DO ESTOQUE */}
      <View style={styles.dashboardContainer}>
        <View style={styles.miniCardDashboard}>
          <Text style={styles.dashboardLabel}>Total de Itens</Text>
          <Text style={styles.dashboardValor}> 350 un</Text>
        </View>
        <View style={styles.miniCardDashboard}>
          <Text style={styles.dashboardLabel}>Valor do Estoque</Text>
          <Text style={[styles.dashboardValor, { color: '#2e7d32' }]}>
            R$ 350.000
          </Text>
        </View>
      </View>


      {/* FORMULÁRIO DE CADASTRO */}
      <View style={styles.cardFormulario}>
        <View style={styles.headerForm}>
          <Text style={styles.tituloForm}>
            {editandoId ? '✏️ EDITAR PRODUTO' : '🌷 NOVO PRODUTO'}
          </Text>
        </View>


        <View style={styles.corpoForm}>
         
          {/* Nova Linha com dois campos menores lado a lado */}
          <View style={styles.linhaFormulario}>
            <View style={{ flex: 1, marginRight: 8 }}>
              <Text style={styles.label}>Código do Poduto *</Text>
              <TextInput
                style={styles.input}
                placeholder="Ex: PROD123"
                autoCapitalize="characters"
                value={sku}
                onChangeText={setSku}
              />
            </View>
            <View style={{ flex: 1, marginLeft: 8 }}>
              <Text style={styles.label}>Qtd. Estoque *</Text>
              <TextInput
                style={styles.input}
                placeholder="Ex: 15"
                keyboardType="numeric"
                value={estoque}
                onChangeText={setEstoque}
              />
            </View>
          </View>


          <Text style={styles.label}>Nome do Produto *</Text>
          <TextInput
            style={styles.input}
            placeholder="Digite o nome do produto"
            value={nome}
            onChangeText={setNome}
          />


          <Text style={styles.label}>Preço Unitário *</Text>
          <TextInput
            style={styles.input}
            placeholder="Digite o preço (ex: 99.90)"
            keyboardType="numeric"
            value={preco}
            onChangeText={setPreco}
          />


          <Text style={styles.label}>Categoria *</Text>
          <View style={styles.containerCategorias}>
            {['Comésticos', 'Maquiagem', 'Perfume', 'Outros'].map((cat) => (
              <TouchableOpacity
                key={cat}
                style={[
                  styles.botaoCategoria,
                  categoria === cat && styles.botaoCategoriaSelecionada
                ]}
                onPress={() => setCategoria(cat)}
              >
                <Text style={[
                  styles.textoCategoria,
                  categoria === cat && styles.textoCategoriaSelecionado
                ]}>
                  {cat}
                </Text>
              </TouchableOpacity>
            ))}
          </View>


          <Text style={styles.label}>Descrição *</Text>
          <TextInput
            style={[styles.input, styles.inputDescricao]}
            placeholder="Digite a descrição detalhada do produto"
            multiline
            numberOfLines={3}
            value={descricao}
            onChangeText={setDescricao}
          />


          {/* BOTÕES DE AÇÃO INTERFACE MELHORADA */}
          <TouchableOpacity style={styles.btnCadastrar} onPress={salvarProduto}>
            <Text style={styles.textBtn}>
              {editandoId ? 'SALVAR ALTERAÇÕES' : 'CADASTRAR NO ESTOQUE'}
            </Text>
          </TouchableOpacity>


          <TouchableOpacity style={styles.btnLimpar} onPress={limparCampos}>
            <Text style={styles.textBtn}>LIMPAR CAMPOS</Text>
          </TouchableOpacity>
        </View>
      </View>


      {/* LISTAGEM DE PRODUTOS CADASTRADOS */}
      <Text style={styles.tituloLista}>📋 Lista de Itens Cadastrados ({produtos.length})</Text>
     
      {produtos.length === 0 ? (
        <Text style={styles.listaVazia}>Nenhum produto cadastrado no momento.</Text>
      ) : (
        <View style={styles.listaContainer}>
          {produtos.map((item) => (
            <View key={item.id} style={styles.itemProduto}>
              <View style={styles.infoProduto}>
                <View style={{ flexDirection: 'row', alignItems: 'center' }}>
                  <Text style={styles.skuTag}>{item.sku}</Text>
                  <Text style={styles.nomeProdLista}>{item.nome}</Text>
                </View>
               
                <Text style={styles.categoriaProdLista}>{item.categoria}</Text>
                <Text style={styles.descProdLista}>{item.descricao}</Text>
               
                {/* Novas Informações no Card da Lista */}
                <View style={styles.linhaInfoCard}>
                  <Text style={styles.precoProdLista}>R$ {item.preco}</Text>
                  <Text style={styles.estoqueProdLista}>Estoque: {item.estoque} un</Text>
                </View>
              </View>
             
              <View style={styles.acoesProduto}>
                <TouchableOpacity
                  style={styles.btnEditar}
                  onPress={() => iniciarEdicao(item)}
                >
                  <Text style={styles.textBtnAcao}>Editar</Text>
                </TouchableOpacity>
                <TouchableOpacity
                  style={styles.btnDeletar}
                  onPress={() => deletarProduto(item.id)}
                >
                  <Text style={styles.textBtnAcao}>Deletar</Text>
                </TouchableOpacity>
              </View>
            </View>
          ))}
        </View>
      )}
     
      <View style={{ height: 50 }} />
    </ScrollView>
  );
}
const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#67008F',
    padding: 16,
    paddingTop: 40,
  },
  tituloPrincipal: {
    fontSize: 22,
    fontWeight: '800',
    color: '#FFFFFF',
    textAlign: 'center',
    marginBottom: 16,
  },
  subtitle: {
    fontSize: 16,
    color: '#DE1FCD',
    textAlign: 'center',
    marginBottom: 50,
  },
  dashboardContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginBottom: 20,
    gap: 12,
  },
  miniCardDashboard: {
    flex: 1,
    backgroundColor: '#fff',
    padding: 12,
    borderRadius: 10,
    alignItems: 'center',
    elevation: 2,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 1 },
    shadowOpacity: 0.1,
    shadowRadius: 2,
  },
  dashboardLabel: {
    fontSize: 12,
    color: '#DE1FCD',
    fontWeight: '600',
    marginBottom: 4,
  },
  dashboardValor: {
    fontSize: 16,
    fontWeight: '700',
    color: '#1e293b',
  },
  cardFormulario: {
    backgroundColor: '#fff',
    borderRadius: 12,
    overflow: 'hidden',
    elevation: 4,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 3 },
    shadowOpacity: 0.15,
    shadowRadius: 5,
    marginBottom: 24,
  },
  headerForm: {
    backgroundColor: '#490A8F',
    padding: 18,
  },
  tituloForm: {
    color: '#ffff',
    fontSize: 16,
    fontWeight: '700',
    textAlign: 'center',
    letterSpacing: 1,
  },
  corpoForm: {
    padding: 18,
  },
  linhaFormulario: {
    flexDirection: 'row',
    justifyContent: 'space-between',
  },
  label: {
    fontSize: 13,
    fontWeight: '700',
    color: '#490A8F',
    marginBottom: 6,
    marginTop: 12,
  },
  input: {
    borderWidth: 1.5,
    borderColor: '#cbd5e1',
    borderRadius: 8,
    padding: 12,
    fontSize: 14,
    backgroundColor: '#f8fafc',
    color: '#1e293b',
  },
  inputDescricao: {
    textAlignVertical: 'top',
    height: 70,
  },
  containerCategorias: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    marginVertical: 6,
    flexWrap: 'wrap',
    gap: 6,
  },
  botaoCategoria: {
    borderWidth: 1.5,
    borderColor: '#cbd5e1',
    borderRadius: 8,
    paddingVertical: 10,
    paddingHorizontal: 8,
    minWidth: '23%',
    alignItems: 'center',
  },
  botaoCategoriaSelecionada: {
    backgroundColor: '#5A0C8A',
    borderColor: '#1e3a8a',
  },
  textoCategoria: {
    fontSize: 12,
    color: '#475569',
    fontWeight: '600',
  },
  textoCategoriaSelecionado: {
    color: '#fff',
  },
  btnCadastrar: {
    backgroundColor: '#490A8F',
    padding: 16,
    borderRadius: 8,
    alignItems: 'center',
    marginTop: 24,
    elevation: 2,
  },
  btnLimpar: {
    backgroundColor: '#490A8F',
    borderWidth: 1,
    borderColor: '#cbd5e1',
    padding: 14,
    borderRadius: 8,
    alignItems: 'center',
    marginTop: 10,
  },
  textBtn: {
    color: '#fff',
    fontWeight: '700',
    fontSize: 14,
    letterSpacing: 0.5,
  },
  tituloLista: {
    fontSize: 18,
    fontWeight: '700',
    color: '#0f172a',
    marginBottom: 14,
  },
   
  listaVazia: {
    textAlign: 'center',
    color: '#64748b',
    fontStyle: 'italic',
    marginTop: 15,
  },
  listaContainer: {
    gap: 14,
  },
  itemProduto: {
    backgroundColor: '#fff',
    padding: 16,
    borderRadius: 12,
    borderLeftWidth: 6,
    borderLeftColor: '#1e3a8a',
    flexDirection: 'row',
    justifyContent: 'space-between',
    elevation: 2,
    shadowColor: '#000',
    shadowOffset: { width: 0, height: 1 },
    shadowOpacity: 0.1,
    shadowRadius: 3,
  },
  infoProduto: {
    flex: 1,
    paddingRight: 10,
  },
  skuTag: {
    fontSize: 11,
    fontWeight: 'bold',
    backgroundColor: '#3b82f6',
    color: '#fff',
    paddingHorizontal: 6,
    paddingVertical: 2,
    borderRadius: 4,
    marginRight: 8,
  },
  nomeProdLista: {
    fontSize: 16,
    fontWeight: '700',
    color: '#1e293b',
    flex: 1,
  },
  categoriaProdLista: {
    fontSize: 11,
    color: '#475569',
    backgroundColor: '#f1f5f9',
    alignSelf: 'flex-start',
    paddingHorizontal: 8,
    paddingVertical: 3,
    borderRadius: 6,
    marginVertical: 6,
    fontWeight: '600',
  },
  descProdLista: {
    fontSize: 13,
    color: '#475569',
    marginBottom: 8,
    lineHeight: 18,
  },
  linhaInfoCard: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: 4,
    borderTopWidth: 1,
    borderTopColor: '#f1f5f9',
    paddingTop: 8,
  },
  precoProdLista: {
    fontSize: 16,
    fontWeight: '800',
    color: '#10b981',
  },
  estoqueProdLista: {
    fontSize: 13,
    fontWeight: '600',
    color: '#64748b',
  },
  acoesProduto: {
    justifyContent: 'center',
    gap: 8,
    minWidth: 70,
  },
  btnEditar: {
    backgroundColor: '#5A0C8A',
    paddingVertical: 8,
    borderRadius: 6,
    alignItems: 'center',
  },
  btnDeletar: {
    backgroundColor: '#5A0C8A',
    paddingVertical: 8,
    borderRadius: 6,
    alignItems: 'center',
  },
  textBtnAcao: {
    color: '#fff',
    fontSize: 12,
    fontWeight: '700',
  },
 

});


**********************************************************************/
INSTITUTO FEDERAL DO AMAZONAS – CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  16/ 06 /2026
Bimestre: 2º Bimestre
Descrição: modificando os projetos 1,2,3.

**********************************************************************/
PROJETO 1:
import React, { useState } from 'react';
import { View, Text, TextInput, Button, Alert, StyleSheet } from 'react-native'; //importa os componentes


export default function App() { //defini a função
  const [idade, setIdade] = useState('');
  const [status, setStatus] = useState('');


  const validarIdade = () => {
    setStatus(''); // Limpa o status anterior
   
    try { //inicia um bloco de tratamentos de erros 
      const idadeNum = parseInt(idade, 10);  


      if (!idade || isNaN(idadeNum)) {
        throw new Error('Por favor, digite uma idade válida.'); // verifica se a validação e verdadeira
      }
      if (idadeNum < 0) {
        throw new Error('Idade não pode ser negativa.');
      }
      if (idadeNum < 18) {
        throw new Error('Entrada proibida: Menor de 18 anos.');
      }


      Alert.alert('Sucesso', 'Entrada liberada! Divirta-se.'); //exibe um alerta
      setStatus('Acesso Autorizado');


    } catch (error) { //captura os erros
      Alert.alert('Erro de Validação', error.message);
      setStatus('Acesso Negado');
    } finally {
      console.log('Validação de idade executada.');
    }
  };


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Validação de Idade</Text>
      <TextInput
        style={styles.input}
        placeholder="Digite sua idade"
        keyboardType="numeric"
        value={idade}
        onChangeText={setIdade}
      />
      <Button title="Verificar Entrada" onPress={validarIdade} color="#1A0AA6" />
      {status ? <Text style={styles.resultado}>Status: {status}</Text> : null}
    </View>
  );
}


const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 20, backgroundColor: '#37A4ED' },
  titulo: { fontSize: 22, fontWeight: 'bold', marginBottom: 20, textAlign: 'center' },
  input: { borderWidth: 1, borderColor: '#1A0AA6', padding: 10, marginBottom: 20, borderRadius: 5, backgroundColor: '#fff' },
  resultado: { marginTop: 20, fontSize: 18, fontWeight: 'bold', textAlign: 'center' },
});




PROJETO 2:
import React, { useState } from 'react';
import { View, Text, TextInput, Button, Alert, StyleSheet } from 'react-native'; //importa os componentes



export default function App() { //defini a funçaõ
  const [saldo, setSaldo] = useState(600.00); // Saldo inicial fixo // declara a função sacar 
  const [valorSaque, setValorSaque] = useState('');


  const realizarSaque = () => {
    try { //inicia um bloco de tratamentos de erros 
      const saque = parseFloat(valorSaque); // declara a função sacar 


      if (!valorSaque || isNaN(saque)) { 
        throw new Error('Digite um valor numérico válido.');
      }
      if (saque <= 0) {
        throw new Error('O valor do saque deve ser maior que zero.');
      }
      if (saque > saldo) { //verifica se o saldo é maior
        throw new Error('Saldo insuficiente para realizar a operação.');
      }


      // Operação matemática
      const novoSaldo = saldo - saque;
      setSaldo(novoSaldo);
      Alert.alert('Sucesso', `Saque de R$ ${saque.toFixed(2)} realizado!`);


    } catch (error) { //captura os erros
      Alert.alert('Erro na Operação', error.message); //exibe o alerta 
    } finally {
      setValorSaque(''); // Limpa o campo de texto sempre
      console.log('Operação de saque finalizada.');
    }
  };


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Saque Bancário</Text>
      <Text style={styles.saldo}>Saldo Atual: R$ {saldo.toFixed(2)}</Text>
      <TextInput
        style={styles.input}
        placeholder="Valor do saque"
        keyboardType="numeric"
        value={valorSaque}
        onChangeText={setValorSaque}
      />
      <Button title="Sacar" onPress={realizarSaque} color="#502687" />
    </View>
  );
}


const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 20, backgroundColor: '#E0B7EB' },
  titulo: { fontSize: 22, fontWeight: 'bold', marginBottom: 10, textAlign: 'center' },
  saldo: { fontSize: 18, marginBottom: 20, textAlign: 'center', color: '#4B115C', fontWeight: 'bold' },
  input: { borderWidth: 1, borderColor: '#a5d6a7', padding: 10, marginBottom: 20, borderRadius: 5, backgroundColor: '#fff' },
});




PROJETO 3:
import React, { useState } from 'react';
import { View, Text, TextInput, Button, Alert, StyleSheet } from 'react-native';


export default function App() {
  const [nota, setNota] = useState('');
  const [notaValida, setNotaValida] = useState(null);


  const validarNota = () => {
    setNotaValida(null);


    try {
      const notaNum = parseFloat(nota);


      if (!nota || isNaN(notaNum)) {
        throw new Error('Insira um número válido.');
      }
      if (notaNum < 0) {
        throw new Error('Erro: A nota não pode ser menor que 0.');
      }
      if (notaNum > 10) {
        throw new Error('Erro: A nota não pode ser maior que 10.');
      }


      // Se passar pelas condições acima, a nota é válida
      setNotaValida(notaNum);


    } catch (error) {
      Alert.alert('Nota Inválida', error.message);
    } finally {
      console.log('Processo de validação de nota concluído.');
    }
  };


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Validador de Notas</Text>
      <TextInput
        style={styles.input}
        placeholder="Digite a nota (0 a 10)"
        keyboardType="numeric"
        value={nota}
        onChangeText={setNota}
      />
      <Button title="Validar Nota" onPress={validarNota} color="#1565c0" />
     
      {notaValida !== null && (
        <View style={styles.boxResultado}>
          <Text style={styles.textoResultado}>Nota válida registrada: {notaValida}</Text>
        </View>
      )}
    </View>
  );
}


const styles = StyleSheet.create({
  container: { flex: 1, justifyContent: 'center', padding: 20, backgroundColor: '#134B8F' },
  titulo: { fontSize: 22, fontWeight: 'bold', marginBottom: 20, textAlign: 'center' },
  input: { borderWidth: 1, borderColor: '#5BA2D9', padding: 10, marginBottom: 20, borderRadius: 5, backgroundColor: '#fff' },
  boxResultado: { marginTop: 30, padding: 15, backgroundColor: '#bbdefb', borderRadius: 5 },
  textoResultado: { fontSize: 16, color: '#0d47a1', fontWeight: 'bold', textAlign: 'center' },
});








**********************************************************************/
INSTITUTO FEDERAL DO AMAZONAS – CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  29/ 07 /2026
Bimestre: 3º Bimestre
**********************************************************************/

Questão 1 – Cadastro Inteligente de Usuários
Uma startup está desenvolvendo um aplicativo de eventos acadêmicos. Durante o cadastro, o
sistema deve validar automaticamente o nome, e-mail e idade do participante.
Desenvolva uma aplicação móvel que:
Possua campos para Nome, E-mail e Idade;
Valide se todos os campos foram preenchidos;
Informe mensagens de erro caso algum dado seja inválido;
Após clicar no botão Cadastrar, apresente uma mensagem personalizada com os
dados do participante.
Considere aspectos relacionados aos eventos de clique e validação da interface.

import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  Button,
  Alert,
  StyleSheet,
} from "react-native";


export default function App() {
  const [nome, setNome] = useState("");
  const [email, setEmail] = useState("");
  const [idade, setIdade] = useState("");


  function cadastrar() {
    if (nome === "" || email === "" || idade === "") {
      Alert.alert("Erro", "Preencha todos os campos!");
      return;
    }


    if (!email.includes("@")) {
      Alert.alert("Erro", "E-mail inválido!");
      return;
    }


    if (parseInt(idade) <= 0) {
      Alert.alert("Erro", "Idade inválida!");
      return;
    }


    Alert.alert(
      "Cadastro realizado!",
      `Nome: ${nome}\nE-mail: ${email}\nIdade: ${idade} anos`
    );
  }


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Cadastro de Usuário</Text>


      <TextInput
        style={styles.input}
        placeholder="Nome"
        value={nome}
        onChangeText={setNome}
      />


      <TextInput
        style={styles.input}
        placeholder="E-mail"
        value={email}
        onChangeText={setEmail}
        keyboardType="email-address"
      />


      <TextInput
        style={styles.input}
        placeholder="Idade"
        value={idade}
        onChangeText={setIdade}
        keyboardType="numeric"
      />


      <Button title="Cadastrar" onPress={cadastrar} />
    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
  },


  titulo: {
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 20,
  },


  input: {
    borderWidth: 1,
    borderColor: "#999",
    borderRadius: 8,
    padding: 10,
    marginBottom: 15,
  },
});




Questão 2 – Controle de Consumo de Água
Uma prefeitura deseja incentivar hábitos sustentáveis através de um aplicativo.
Desenvolva uma aplicação que permita:
Informar a quantidade de litros consumidos por dia;
Ao pressionar um botão, calcular o consumo semanal;

Classificar o consumo em:
o Econômico;
o Moderado;
o Elevado;
Exibir o resultado na tela.

import React, { useState } from 'react';
import {
  View,
  Text,
  TextInput,
  Button,
  StyleSheet
} from 'react-native';




export default function App() {
  const [litros, setLitros] = useState('');
  const [resultado, setResultado] = useState('');




  function calcularConsumo() {
    if (litros === '') {
      setResultado('Informe a quantidade de litros.');
      return;
    }




    const consumoDia = parseFloat(litros);




    if (isNaN(consumoDia) || consumoDia <= 0) {
      setResultado('Valor inválido.');
      return;
    }




    const semanal = consumoDia * 7;
    let classificacao = '';




    if (semanal <= 350) {
      classificacao = 'Econômico';
    } else if (semanal <= 700) {
      classificacao = 'Moderado';
    } else {
      classificacao = 'Elevado';
    }




    setResultado(
      `Consumo semanal: ${semanal.toFixed(1)} litros\nClassificação: ${classificacao}`
    );
  }




  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>
        Controle de Consumo de Água
      </Text>




      <TextInput
        style={styles.input}
        placeholder="Litros consumidos por dia"
        keyboardType="numeric"
        value={litros}
        onChangeText={setLitros}
      />




      <Button
        title="Calcular Consumo"
        onPress={calcularConsumo}
      />




      <Text style={styles.resultado}>
        {resultado}
      </Text>
    </View>
  );
}




const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    padding: 20,
     backgroundColor: '#7995E8',
  },




  titulo: {
    fontSize: 24,
    fontWeight: 'bold',
    textAlign: 'center',
    marginBottom: 20,
     padding: 20,
     backgroundColor: '#3B9BDB',
  },




  input: {
    borderWidth: 1,
    borderColor: '#999',
    borderRadius: 8,
    padding: 10,
    marginBottom: 20,
     backgroundColor: '#3B9BDB',
  },




  resultado: {
    marginTop: 20,
    fontSize: 18,
    textAlign: 'center',
     padding: 20,
     backgroundColor: '#3B9BDB',
  },
});




Questão 3 – Calculadora de IMC
Uma clínica de saúde pretende disponibilizar um aplicativo para cálculo do Índice de Massa
Corporal.
Sua aplicação deverá:
Receber peso e altura;
Executar o cálculo do IMC;
Informar automaticamente a classificação correspondente;
Utilizar um botão para iniciar o cálculo.
Considere boas práticas na interação entre usuário e interface.
import React, { useState } from "react";
import { View, Text, TextInput, Button, StyleSheet } from "react-native";


export default function App() {
  const [peso, setPeso] = useState("");
  const [altura, setAltura] = useState("");
  const [resultado, setResultado] = useState("");


  function calcularIMC() {
    if (peso === "" || altura === "") {
      setResultado("Preencha todos os campos.");
      return;
    }


    const p = parseFloat(peso);
    const a = parseFloat(altura);


    if (isNaN(p) || isNaN(a) || p <= 0 || a <= 0) {
      setResultado("Valores inválidos.");
      return;
    }


    const imc = p / (a * a);
    let classificacao = "";


    if (imc < 18.5) {
      classificacao = "Abaixo do peso";
    } else if (imc < 25) {
      classificacao = "Peso normal";
    } else if (imc < 30) {
      classificacao = "Sobrepeso";
    } else if (imc < 35) {
      classificacao = "Obesidade Grau I";
    } else if (imc < 40) {
      classificacao = "Obesidade Grau II";
    } else {
      classificacao = "Obesidade Grau III";
    }


    setResultado(
      `IMC: ${imc.toFixed(2)}\nClassificação: ${classificacao}`
    );
  }


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Calculadora de IMC</Text>


      <TextInput
        style={styles.input}
        placeholder="Peso (kg)"
        keyboardType="numeric"
        value={peso}
        onChangeText={setPeso}
      />


      <TextInput
        style={styles.input}
        placeholder="Altura (m)"
        keyboardType="numeric"
        value={altura}
        onChangeText={setAltura}
      />


      <Button title="Calcular IMC" onPress={calcularIMC} />


      <Text style={styles.resultado}>{resultado}</Text>
    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
    backgroundColor: '#2773F5'
  },


  titulo: {
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 20,
    padding: 20,
    backgroundColor: '#27ADF5'
  },


  input: {
    borderWidth: 1,
    borderColor: "#999",
    borderRadius: 8,
    padding: 10,
    marginBottom: 15,
    backgroundColor: '#27ADF5'
  },


  resultado: {
    marginTop: 20,
    fontSize: 18,
    textAlign: "center",
    padding: 20,
    backgroundColor: '#27ADF5'
  },
});



Questão 4 – Conversor de Temperatura
Um aplicativo meteorológico necessita converter temperaturas entre Celsius e Fahrenheit.
Desenvolva uma aplicação que:
Receba uma temperatura em Celsius;
Ao clicar em um botão, converta para Fahrenheit;
Exiba o resultado imediatamente;
Possua um botão para limpar os campos.
Utilize corretamente eventos de clique.

import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  Button,
  StyleSheet,
} from "react-native";


export default function App() {
  const [celsius, setCelsius] = useState("");
  const [resultado, setResultado] = useState("");


  function converter() {
    if (celsius === "") {
      setResultado("Digite uma temperatura.");
      return;
    }


    const c = parseFloat(celsius);


    if (isNaN(c)) {
      setResultado("Valor inválido.");
      return;
    }


    const fahrenheit = (c * 9) / 5 + 32;
    const kelvin = c + 273.15;


    setResultado(
      `Fahrenheit: ${fahrenheit.toFixed(2)} °F\nKelvin: ${kelvin.toFixed(2)} K`
    );
  }


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Conversor de Temperatura</Text>


      <TextInput
        style={styles.input}
        placeholder="Temperatura em °C"
        keyboardType="numeric"
        value={celsius}
        onChangeText={setCelsius}
      />


      <Button
        title="Converter"
        onPress={converter}
      />


      <Text style={styles.resultado}>
        {resultado}
      </Text>
    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
    backgroundColor: '#48036B'
  },


  titulo: {
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 20,
    padding: 20,
    backgroundColor: '#E0B7EB'
  },


  input: {
    borderWidth: 1,
    borderColor: "#999",
    borderRadius: 8,
    padding: 10,
    marginBottom: 20,
    backgroundColor: '#E0B7EB'
  },


  resultado: {
    marginTop: 20,
    fontSize: 18,
    textAlign: "center",
    padding: 20,
    backgroundColor: '#E0B7EB'
  },
});



Questão 5 – Sistema de Login
Uma empresa pretende criar um aplicativo interno para seus funcionários.
Desenvolva uma aplicação contendo:
Campo Usuário;
Campo Senha;
Botão Entrar.

Considere que:
Usuário: admin
Senha: 1234
Caso os dados estejam corretos, exiba uma mensagem de boas-vindas. Caso contrário,
informe que o acesso foi negado.


import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  Button,
  StyleSheet,
  Alert,
} from "react-native";


export default function App() {
  const [usuario, setUsuario] = useState("");
  const [senha, setSenha] = useState("");


  function entrar() {
    if (usuario === "" || senha === "") {
      Alert.alert("Erro", "Preencha todos os campos.");
      return;
    }


    if (usuario === "admin" && senha === "1234") {
      Alert.alert("Sucesso", "Login realizado com sucesso!");
    } else {
      Alert.alert("Erro", "Usuário ou senha incorretos.");
    }
  }


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Sistema de Login</Text>


      <TextInput
        style={styles.input}
        placeholder="Usuário"
        value={usuario}
        onChangeText={setUsuario}
      />


      <TextInput
        style={styles.input}
        placeholder="Senha"
        secureTextEntry={true}
        value={senha}
        onChangeText={setSenha}
      />


      <Button
        title="Entrar"
        onPress={entrar}
      />
    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
    backgroundColor: '#1F20A6'
  },


  titulo: {
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 20,
    padding: 20,
    backgroundColor: '#3839EB'
  },


  input: {
    borderWidth: 1,
    borderColor: "#999",
    borderRadius: 8,
    padding: 10,
    marginBottom: 15,
    backgroundColor: '#3839EB'
  },
});



Questão 6 – Simulador de Caixa Eletrônico
Uma instituição financeira deseja disponibilizar um simulador de saque.
Desenvolva uma aplicação que:
 Receba um valor de saque;
 Verifique se o valor é múltiplo de R$10;
 Caso seja válido, informe "Saque autorizado";
 Caso contrário, informe que o valor é inválido.
Utilize tratamento de eventos e estruturas condicionais.


import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  Button,
  StyleSheet,
  Alert,
} from "react-native";


export default function App() {
  const [saldo, setSaldo] = useState(1000);
  const [valor, setValor] = useState("");


  function sacar() {
    if (valor === "") {
      Alert.alert("Erro", "Informe um valor.");
      return;
    }


    const saque = parseFloat(valor);


    if (isNaN(saque) || saque <= 0) {
      Alert.alert("Erro", "Valor inválido.");
      return;
    }


    if (saque > saldo) {
      Alert.alert("Erro", "Saldo insuficiente.");
      return;
    }


    setSaldo(saldo - saque);
    Alert.alert("Sucesso", `Saque de R$ ${saque.toFixed(2)} realizado.`);
    setValor("");
  }


  function depositar() {
    if (valor === "") {
      Alert.alert("Erro", "Informe um valor.");
      return;
    }


    const deposito = parseFloat(valor);


    if (isNaN(deposito) || deposito <= 0) {
      Alert.alert("Erro", "Valor inválido.");
      return;
    }


    setSaldo(saldo + deposito);
    Alert.alert("Sucesso", `Depósito de R$ ${deposito.toFixed(2)} realizado.`);
    setValor("");
  }


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Caixa Eletrônico</Text>


      <Text style={styles.saldo}>
        Saldo: R$ {saldo.toFixed(2)}
      </Text>


      <TextInput
        style={styles.input}
        placeholder="Digite o valor"
        keyboardType="numeric"
        value={valor}
        onChangeText={setValor}
      />


      <View style={styles.botoes}>
        <Button title="Depositar" onPress={depositar} />
      </View>


      <View style={styles.botoes}>
        <Button title="Sacar" onPress={sacar} />
      </View>
    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
  },


  titulo: {
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 20,
  },


  saldo: {
    fontSize: 20,
    textAlign: "center",
    marginBottom: 20,
  },


  input: {
    borderWidth: 1,
    borderColor: "#999",
    borderRadius: 8,
    padding: 10,
    marginBottom: 20,
  },


  botoes: {
    marginBottom: 10,
  },
});



Questão 7 – Aplicativo de Lista de Compras
Um supermercado deseja um aplicativo simples para organizar compras.
Desenvolva uma aplicação que permita:
Inserir produtos em uma lista;
Adicionar novos itens através de um botão;
Exibir todos os produtos cadastrados;
Permitir limpar completamente a lista.
Utilize componentes de lista e eventos de clique.

import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  Button,
  FlatList,
  StyleSheet,
} from "react-native";


export default function App() {
  const [produto, setProduto] = useState("");
  const [lista, setLista] = useState([]);


  function adicionarProduto() {
    if (produto.trim() === "") {
      return;
    }


    setLista([...lista, { id: Date.now().toString(), nome: produto }]);
    setProduto("");
  }


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Lista de Compras</Text>


      <TextInput
        style={styles.input}
        placeholder="Digite um produto"
        value={produto}
        onChangeText={setProduto}
      />


      <Button
        title="Adicionar Produto"
        onPress={adicionarProduto}
      />


      <FlatList
        data={lista}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => (
          <Text style={styles.item}>• {item.nome}</Text>
        )}
      />
    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 20,
    marginTop: 50,
    backgroundColor: '#540F7D'
  },


  titulo: {
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 20,
    padding: 20,
    backgroundColor: '#AC60DB'
  },


  input: {
    borderWidth: 1,
    borderColor: "#999",
    borderRadius: 8,
    padding: 10,
    marginBottom: 15,
    backgroundColor: '#E0B7EB',
  },


  item: {
    fontSize: 18,
    padding: 8,
    borderBottomWidth: 1,
    borderBottomColor: "#ddd",
    backgroundColor: '#E0B7EB',
  },
});


Questão 8 – Contador de Cliques
Uma empresa de testes de interface deseja medir a interação dos usuários com um botão.
Desenvolva uma aplicação que:
Possua um botão "Clique Aqui";
Conte quantas vezes o botão foi pressionado;
Atualize automaticamente a quantidade de cliques na tela;
Tenha um botão para reiniciar o contador.
import React, { useState } from "react";
import {
  View,
  Text,
  Button,
  StyleSheet,
} from "react-native";


export default function App() {
  const [contador, setContador] = useState(0);


  function incrementar() {
    setContador(contador + 1);
  }


  function decrementar() {
    setContador(contador - 1);
  }


  function zerar() {
    setContador(0);
  }


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Contador de Cliques</Text>


      <Text style={styles.contador}>{contador}</Text>


      <View style={styles.botao}>
        <Button
          title="Incrementar"
          onPress={incrementar}
        />
      </View>


      <View style={styles.botao}>
        <Button
          title="Decrementar"
          onPress={decrementar}
        />
      </View>


      <View style={styles.botao}>
        <Button
          title="Zerar"
          onPress={zerar}
        />
      </View>
    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
  },


  titulo: {
    fontSize: 26,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 20,
  },


  contador: {
    fontSize: 50,
    textAlign: "center",
    marginBottom: 30,
    fontWeight: "bold",
  },


  botao: {
    marginBottom: 15,
  },
});




Questão 9 – Escolha de Plano de Internet
Uma operadora deseja disponibilizar um simulador de contratação.
Crie uma aplicação contendo três opções de plano:
Básico
Intermediário
Premium
Ao selecionar um plano e pressionar um botão, apresente:
Nome do plano;
Valor mensal;
Benefícios principais.
Utilize componentes de seleção e tratamento de eventos.

import React, { useState } from "react";
import {
  View,
  Text,
  Button,
  StyleSheet,
} from "react-native";


export default function App() {
  const [plano, setPlano] = useState("");


  function escolherPlano(tipo) {
    setPlano(tipo);
  }


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Escolha seu Plano</Text>


      <View style={styles.botao}>
        <Button
          title="Plano Básico"
          onPress={() => escolherPlano("Plano Básico\n100 MB\nR$ 59,90/mês")}
        />
      </View>


      <View style={styles.botao}>
        <Button
          title="Plano Intermediário"
          onPress={() =>
            escolherPlano("Plano Intermediário\n300 MB\nR$ 89,90/mês")
          }
        />
      </View>


      <View style={styles.botao}>
        <Button
          title="Plano Premium"
          onPress={() =>
            escolherPlano("Plano Premium\n600 MB\nR$ 129,90/mês")
          }
        />
      </View>


      <Text style={styles.resultado}>{plano}</Text>
    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
  },


  titulo: {
    fontSize: 26,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 25,
  },


  botao: {
    marginBottom: 15,
  },


  resultado: {
    marginTop: 30,
    fontSize: 20,
    textAlign: "center",
    fontWeight: "bold",
  },
});


Questão 10 – Aplicativo de Controle Escolar
Uma escola pretende desenvolver um aplicativo para cálculo da média dos alunos.
A aplicação deverá:
Receber três notas;
Calcular automaticamente a média;
Informar:
o Aprovado;
o Recuperação;
o Reprovado.

O cálculo deverá ocorrer quando o usuário pressionar um botão.
Além disso, implemente um botão para limpar todos os campos.
import React, { useState } from "react";
import {
  View,
  Text,
  TextInput,
  Button,
  StyleSheet,
} from "react-native";


export default function App() {
  const [nome, setNome] = useState("");
  const [nota1, setNota1] = useState("");
  const [nota2, setNota2] = useState("");
  const [resultado, setResultado] = useState("");


  function calcularMedia() {
    if (nome === "" || nota1 === "" || nota2 === "") {
      setResultado("Preencha todos os campos.");
      return;
    }


    const n1 = parseFloat(nota1);
    const n2 = parseFloat(nota2);


    if (isNaN(n1) || isNaN(n2)) {
      setResultado("Digite notas válidas.");
      return;
    }


    const media = (n1 + n2) / 2;


    let situacao = "";


    if (media >= 7) {
      situacao = "Aprovado";
    } else if (media >= 5) {
      situacao = "Recuperação";
    } else {
      situacao = "Reprovado";
    }


    setResultado(
      `Aluno: ${nome}\nMédia: ${media.toFixed(1)}\nSituação: ${situacao}`
    );
  }


  return (
    <View style={styles.container}>
      <Text style={styles.titulo}>Controle Escolar</Text>


      <TextInput
        style={styles.input}
        placeholder="Nome do aluno"
        value={nome}
        onChangeText={setNome}
      />


      <TextInput
        style={styles.input}
        placeholder="1ª Nota"
        keyboardType="numeric"
        value={nota1}
        onChangeText={setNota1}
      />


      <TextInput
        style={styles.input}
        placeholder="2ª Nota"
        keyboardType="numeric"
        value={nota2}
        onChangeText={setNota2}
      />


      <Button
        title="Calcular Média"
        onPress={calcularMedia}
      />


      <Text style={styles.resultado}>
        {resultado}
      </Text>
    </View>
  );
}


const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
    padding: 20,
    backgroundColor: '#4A8BF7',
  },


  titulo: {
    fontSize: 24,
    fontWeight: "bold",
    textAlign: "center",
    marginBottom: 20,
  },


  input: {
    borderWidth: 1,
    borderColor: "#999",
    borderRadius: 8,
    padding: 10,
    marginBottom: 15,
  },


  resultado: {
    marginTop: 20,
    fontSize: 18,
    textAlign: "center",
  },
});







**********************************************************************/
INSTITUTO FEDERAL DO AMAZONAS – CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  12/ 08 /2026
Bimestre: 3º Bimestre
descrição: implementar I.a no projeto consumo de agua
**********************************************************************/



import React, { useState, useEffect } from 'react';
import {
  StyleSheet,
  Text,
  View,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
  ScrollView,
  Keyboard,
  ActivityIndicator,
  Alert,
  StatusBar,
} from 'react-native';
import AsyncStorage from '@react-native-async-storage/async-storage';

// =====================================================
// OPENROUTER
// =====================================================
const OPENROUTER_API_KEY = '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'; 
const MODEL = 'openai/gpt-4o-mini';

const STORAGE_KEY = '@ecomonitor_historico';

export default function App() {
  const [litrosDiarios, setLitrosDiarios] = useState('');
  const [carregando, setCarregando] = useState(false);
  const [resultadoIA, setResultadoIA] = useState(null);
  const [historico, setHistorico] = useState([]);

  useEffect(() => {
    carregarHistorico();
  }, []);

  const carregarHistorico = async () => {
    try {
      const dados = await AsyncStorage.getItem(STORAGE_KEY);
      if (dados) {
        setHistorico(JSON.parse(dados));
      }
    } catch (erro) {
      console.log('Erro ao carregar histórico:', erro);
    }
  };

  const salvarNoHistorico = async (analise) => {
    try {
      const novoItem = {
        id: Date.now().toString(),
        ...analise,
        criado_em: new Date().toISOString(),
      };

      const dados = await AsyncStorage.getItem(STORAGE_KEY);
      const lista = dados ? JSON.parse(dados) : [];
      const novaLista = [novoItem, ...lista];

      await AsyncStorage.setItem(STORAGE_KEY, JSON.stringify(novaLista));
      setHistorico(novaLista);
    } catch (erro) {
      console.log('Erro ao salvar:', erro);
    }
  };

  const deletarAnalise = async (id) => {
    try {
      const novaLista = historico.filter((item) => item.id !== id);
      await AsyncStorage.setItem(STORAGE_KEY, JSON.stringify(novaLista));
      setHistorico(novaLista);
    } catch (erro) {
      console.log('Erro ao deletar:', erro);
    }
  };

  const limparHistorico = async () => {
    try {
      await AsyncStorage.removeItem(STORAGE_KEY);
      setHistorico([]);
    } catch (erro) {
      console.log('Erro ao limpar:', erro);
    }
  };

  const converterResposta = (texto) => {
    if (!texto) throw new Error('A IA não retornou nenhum conteúdo.');

    let textoLimpo = texto
      .trim()
      .replace(/^```json\s*/i, '')
      .replace(/^```\s*/i, '')
      .replace(/\s*```$/i, '')
      .trim();

    try {
      return JSON.parse(textoLimpo);
    } catch {
      throw new Error('A IA retornou um formato inválido.');
    }
  };

  const analisarConsumoComIA = async () => {
    if (!litrosDiarios.trim()) {
      Alert.alert('Atenção', 'Digite o consumo diário de água.');
      return;
    }

    const litros = parseFloat(litrosDiarios.replace(',', '.'));

    if (isNaN(litros) || litros <= 0) {
      Alert.alert('Atenção', 'Digite um número válido maior que zero.');
      return;
    }

    if (
      !OPENROUTER_API_KEY ||
      OPENROUTER_API_KEY.includes('SUA_CHAVE') ||
      OPENROUTER_API_KEY.includes(' ')
    ) {
      Alert.alert(
        'API não configurada',
        'Coloque sua chave do OpenRouter na variável OPENROUTER_API_KEY.'
      );
      return;
    }

    Keyboard.dismiss();
    setCarregando(true);
    setResultadoIA(null);

    const prompt = `
Você é a IA do aplicativo EcoMonitor IA.
O usuário informou um consumo de ${litros} litros de água por dia.

Faça uma análise simples.
Retorne SOMENTE JSON válido nesta estrutura:

{
  "semanal": "1050 litros",
  "classificacao": "Consumo moderado",
  "cor": "#3F51B5",
  "descricao": "Explicação curta do que o consumo significa.",
  "dica": "Dica prática para economizar água."
}

Não coloque texto fora do JSON.
`;

    try {
      const resposta = await fetch(
        'https://openrouter.ai/api/v1/chat/completions',
        {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            Authorization: `Bearer ${OPENROUTER_API_KEY}`,
            'HTTP-Referer': 'https://snack.expo.dev',
            'X-Title': 'EcoMonitor IA',
          },
          body: JSON.stringify({
            model: MODEL,
            messages: [{ role: 'user', content: prompt }],
            response_format: { type: 'json_object' },
            max_tokens: 400,
            temperature: 0.3,
          }),
        }
      );

      const dados = await resposta.json();

      if (!resposta.ok) {
        throw new Error(
          dados?.error?.message || `Erro HTTP ${resposta.status}`
        );
      }

      const textoIA = dados?.choices?.[0]?.message?.content;
      if (!textoIA) {
        throw new Error('A IA não retornou resposta.');
      }

      const resultado = converterResposta(textoIA);

      const resultadoFinal = {
        litrosDiarios: litros,
        semanal: resultado.semanal || `${litros * 7} litros`,
        classificacao: resultado.classificacao || 'Consumo analisado',
        cor: resultado.cor || '#3F51B5',
        descricao:
          resultado.descricao ||
          'A IA analisou o consumo informado.',
        dica: resultado.dica || 'Evite desperdícios de água.',
      };

      setResultadoIA(resultadoFinal);
      await salvarNoHistorico(resultadoFinal);
    } catch (erro) {
      console.log('ERRO:', erro);
      Alert.alert(
        'Erro',
        erro?.message || 'Não foi possível conectar ao OpenRouter.'
      );
    } finally {
      setCarregando(false);
    }
  };

  const novaAnalise = () => {
    setResultadoIA(null);
    setLitrosDiarios('');
  };

  const confirmarLimpar = () => {
    Alert.alert('Limpar histórico', 'Apagar todas as análises?', [
      { text: 'Cancelar', style: 'cancel' },
      {
        text: 'Apagar',
        style: 'destructive',
        onPress: limparHistorico,
      },
    ]);
  };

  const confirmarDeletar = (id) => {
    Alert.alert('Excluir', 'Remover esta análise?', [
      { text: 'Cancelar', style: 'cancel' },
      {
        text: 'Excluir',
        style: 'destructive',
        onPress: () => deletarAnalise(id),
      },
    ]);
  };

  const formatarData = (iso) => {
    try {
      return new Date(iso).toLocaleString('pt-BR', {
        day: '2-digit',
        month: '2-digit',
        year: 'numeric',
        hour: '2-digit',
        minute: '2-digit',
      });
    } catch {
      return '';
    }
  };

  return (
    <SafeAreaView style={styles.container}>
      <StatusBar barStyle="dark-content" backgroundColor="#F0F2F5" />

      <ScrollView
        contentContainerStyle={styles.scrollContent}
        keyboardShouldPersistTaps="handled"
      >
        <View style={styles.header}>
          <Text style={styles.title}>EcoMonitor IA</Text>
          <Text style={styles.subtitle}>Monitore seu consumo de água</Text>
        </View>

        <View style={styles.card}>
          <Text style={styles.label}>Consumo diário</Text>
          <Text style={styles.description}>
            Informe quantos litros de água são consumidos por dia.
          </Text>

          <TextInput
            style={styles.input}
            placeholder="Ex: 150"
            placeholderTextColor="#999"
            keyboardType="decimal-pad"
            value={litrosDiarios}
            onChangeText={setLitrosDiarios}
            editable={!carregando}
          />
          <Text style={styles.unit}>litros por dia</Text>

          <TouchableOpacity
            style={[styles.button, carregando && styles.buttonDisabled]}
            onPress={analisarConsumoComIA}
            disabled={carregando}
            activeOpacity={0.8}
          >
            {carregando ? (
              <>
                <ActivityIndicator color="#FFF" size="small" />
                <Text style={styles.buttonText}>Analisando...</Text>
              </>
            ) : (
              <Text style={styles.buttonText}>Analisar consumo</Text>
            )}
          </TouchableOpacity>
        </View>

        {resultadoIA && (
          <View
            style={[styles.resultCard, { borderLeftColor: resultadoIA.cor }]}
          >
            <Text style={styles.resultHeader}>RESULTADO DA IA</Text>
            <Text style={[styles.resultTitle, { color: resultadoIA.cor }]}>
              {resultadoIA.classificacao}
            </Text>

            <View style={styles.divider} />

            <Text style={styles.resultLabel}>Consumo semanal</Text>
            <Text style={styles.weeklyValue}>{resultadoIA.semanal}</Text>

            <Text style={styles.resultLabel}> O que aconteceu</Text>
            <Text style={styles.feedbackText}>{resultadoIA.descricao}</Text>

            <Text style={styles.resultLabel}> Dica</Text>
            <Text style={styles.feedbackText}>{resultadoIA.dica}</Text>

            <TouchableOpacity style={styles.newButton} onPress={novaAnalise}>
              <Text style={styles.newButtonText}>Nova análise</Text>
            </TouchableOpacity>
          </View>
        )}

        <View style={styles.historicoHeader}>
          <Text style={styles.historicoTitle}>Histórico</Text>
          {historico.length > 0 && (
            <TouchableOpacity onPress={confirmarLimpar}>
              <Text style={styles.limparTexto}>Limpar</Text>
            </TouchableOpacity>
          )}
        </View>

        {historico.length === 0 ? (
          <View style={styles.infoCard}>
            <Text style={styles.infoText}>
              Nenhuma análise salva ainda.
            </Text>
          </View>
        ) : (
          historico.map((item) => (
            <View
              key={item.id}
              style={[styles.historicoCard, { borderLeftColor: item.cor }]}
            >
              <View style={styles.historicoTop}>
                <Text style={[styles.historicoClass, { color: item.cor }]}>
                  {item.classificacao}
                </Text>
                <TouchableOpacity onPress={() => confirmarDeletar(item.id)}>
                  <Text style={styles.deletarTexto}>Excluir</Text>
                </TouchableOpacity>
              </View>

              <Text style={styles.historicoLitros}>
                {item.litrosDiarios} L/dia → {item.semanal}
              </Text>

              {item.descricao ? (
                <Text style={styles.historicoDesc} numberOfLines={2}>
                  {item.descricao}
                </Text>
              ) : null}

              <Text style={styles.historicoData}>
                {formatarData(item.criado_em)}
              </Text>
            </View>
          ))
        )}

        <View style={styles.infoCard}>
          <Text style={styles.infoTitle}> EcoMonitor IA</Text>
          <Text style={styles.infoText}>
            Histórico salvo no celular com AsyncStorage (funciona no Snack).
          </Text>
        </View>
      </ScrollView>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: '#F0F2F5' },
  scrollContent: { padding: 20, paddingBottom: 40 },
  header: { alignItems: 'center', marginTop: 20, marginBottom: 25 },
  title: { fontSize: 28, fontWeight: '800', color: '#1A237E' },
  subtitle: { marginTop: 6, fontSize: 14, color: '#777' },
  card: {
    backgroundColor: '#FFF',
    borderRadius: 18,
    padding: 20,
    elevation: 4,
    shadowColor: '#000',
    shadowOpacity: 0.1,
    shadowRadius: 6,
    shadowOffset: { width: 0, height: 2 },
  },
  label: { fontSize: 17, fontWeight: '700', color: '#222', marginBottom: 6 },
  description: { fontSize: 14, color: '#777', marginBottom: 14, lineHeight: 20 },
  input: {
    borderWidth: 1.5,
    borderColor: '#E0E0E0',
    borderRadius: 12,
    paddingHorizontal: 14,
    paddingVertical: 14,
    fontSize: 17,
    backgroundColor: '#FAFAFA',
    color: '#222',
  },
  unit: { fontSize: 12, color: '#888', marginTop: 4, marginBottom: 16 },
  button: {
    backgroundColor: '#3F51B5',
    paddingVertical: 16,
    borderRadius: 12,
    alignItems: 'center',
    flexDirection: 'row',
    justifyContent: 'center',
    gap: 10,
  },
  buttonDisabled: { opacity: 0.7 },
  buttonText: { color: '#FFF', fontSize: 16, fontWeight: 'bold' },
  resultCard: {
    backgroundColor: '#FFF',
    borderRadius: 14,
    padding: 18,
    marginTop: 22,
    borderLeftWidth: 7,
    elevation: 3,
  },
  resultHeader: { fontSize: 12, fontWeight: '700', color: '#888', marginBottom: 6 },
  resultTitle: { fontSize: 20, fontWeight: '800' },
  divider: { height: 1, backgroundColor: '#EEE', marginVertical: 14 },
  resultLabel: { fontSize: 13, fontWeight: '700', color: '#666', marginTop: 8 },
  weeklyValue: {
    fontSize: 22,
    fontWeight: '800',
    color: '#1A237E',
    marginTop: 4,
    marginBottom: 12,
  },
  feedbackText: { marginTop: 6, fontSize: 15, lineHeight: 21, color: '#333' },
  newButton: {
    marginTop: 18,
    paddingVertical: 12,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: '#3F51B5',
    alignItems: 'center',
  },
  newButtonText: { color: '#3F51B5', fontWeight: '700', fontSize: 14 },
  historicoHeader: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    marginTop: 28,
    marginBottom: 12,
  },
  historicoTitle: { fontSize: 17, fontWeight: '700', color: '#1A237E' },
  limparTexto: { color: '#E53935', fontWeight: '600', fontSize: 14 },
  historicoCard: {
    backgroundColor: '#FFF',
    borderRadius: 12,
    padding: 14,
    marginBottom: 10,
    borderLeftWidth: 5,
  },
  historicoTop: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
  },
  historicoClass: { fontSize: 15, fontWeight: '700' },
  deletarTexto: { color: '#E53935', fontSize: 13, fontWeight: '600' },
  historicoLitros: { marginTop: 5, fontSize: 14, color: '#444', fontWeight: '600' },
  historicoDesc: { marginTop: 5, fontSize: 13, color: '#666', lineHeight: 18 },
  historicoData: { marginTop: 7, fontSize: 12, color: '#999' },
  infoCard: {
    marginTop: 18,
    padding: 16,
    backgroundColor: '#E8EAF6',
    borderRadius: 14,
  },
  infoTitle: { fontSize: 15, fontWeight: '700', color: '#1A237E', marginBottom: 6 },
  infoText: { fontSize: 13, lineHeight: 18, color: '#4A4A4A' },
});



**********************************************************************/
INSTITUTO FEDERAL DO AMAZONAS – CAMPUS IRANDUBA
DISCIPLINA: Dispositivos Móveis
PROFESSOR: Ranyere Lima
ALUNO(A): Ana Moser M. de Oliveira
DATA:  26/ 08 /2026
Bimestre: 3º Bimestre
descrição: RECONHECER O EVENTO
**********************************************************************/
